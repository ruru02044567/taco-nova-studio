# 下次開機接手

## 🔴 最優先：D4S1 只差一步就能發

**片子做好了，卡在一個東西：麵粉袋上的 Gold Medal 商標。**

| 檔案 | 說明 |
|---|---|
| `待審核\d4s1-麵粉-v2-無幻覺.mp4` | **最新版**，10.08 秒／1080×1920／−14.7 LUFS |
| `待審核\d4s1-v2-畫面.jpg` | 四格截圖，快速看畫面 |
| `待審核\d4s1-麵粉-有聲.mp4` | 舊版（有幻覺幼犬，**別發這支**） |

標題與文案**已經改好並通過格式檢查**，在 `auto\clips\d4s1_title.txt` / `d4s1_desc.txt`。

### 差的那一步：換一張沒有品牌的場景圖

`schedule.json` 的 `scenePrompt` **已經加好禁令**（PLAIN UNBRANDED、no logo、no nutrition panel），
所以只要 Gemini 能用，重生就會生對：

```bash
cd "C:\Users\TUF Gaming\Desktop\我的專案\財富密碼\auto"
python make_scene.py clips/d4s1_scene.txt clips/d4s1_scene.jpg
# 生完檢查袋子有沒有商標，沒有的話重生兩段：
python make_video_local_5s.py clips/d4s1_scene.jpg clips/d4s1_v2_seg1.txt clips/d4s1_v2_seg1.mp4
ffmpeg -y -sseof -0.12 -i clips/d4s1_v2_seg1.mp4 -frames:v 1 -update 1 clips/d4s1_v2_bridge.jpg
python make_video_local_5s.py clips/d4s1_v2_bridge.jpg clips/d4s1_v2_seg2.txt clips/d4s1_v2_seg2.mp4
# 接起來 → 配音效（配方叫 flour）→ 補增益到 -14
```

⚠️ 8/11 傍晚一直搶不到瀏覽器（隔壁卡通農場視窗在跑 `run_ab` 和 `diag4`），
所以這張圖沒生成。**互斥鎖是對的，不要硬搶** —— 那是今天為了解決「兩邊互相沖掉對方作業」才加的。

**三條備案都試過了，結論**：

| 做法 | 結果 |
|---|---|
| 矩形模糊 | 硬邊像打碼，Nutrition Facts 還露在外 |
| 景深模糊（sigma 9） | 商標糊掉了，但右側下方有可見分界。**可接受但不完美** |
| 本機重生場景圖 | ❌ 黑點眉消失、哈士奇變白狗、地上出現藍色碎片，**退化太多，否決** |

真的趕時間就用景深模糊那版：

```bash
ffmpeg -y -i 輸入.mp4 -vf "split[a][b];[b]crop=400:420:0:1200,gblur=sigma=9[bl];[a][bl]overlay=0:1200:enable='between(t,0,3.5)'" -c:a copy 輸出.mp4
```

---

## 今天（8/11）的重要成果

### 1. 三方審片機制建立了，而且第一次用就抓到我漏看的東西

派三個互不通氣的子代理獨立審片（畫面品質／對標比較／合規風險），最後合議。
**它抓到我沒看見的幻覺幼犬** —— 麵粉堆裡有一隻小白狗的頭（鼻子、眼點、四趾爪），
佔畫面 1/4、持續 7 秒。我做七項驗收時把它當成紙袋皺褶，判了通過。

報告在 `待審核\審片-*.md`。

⚠️ 三位對同一個瑕疵給了「勉強可發」和「致命擋發」兩種結論，
原因是**沒人講在什麼尺寸下看**。下次派審片代理，prompt 要寫死
「判斷基準＝6 吋手機全螢幕、不暫停」。

### 2. 幻覺幼犬的解法（已寫進 `make_video_local_5s.py`）

原本的負面詞有 `three dogs / extra dog` 但擋不住 —— 因為它不是一隻完整的狗，
是「材料堆裡浮現出動物的形狀」。要直接點名：

```
animal shape hidden in the pile, face emerging from powder, puppy in the flour,
creature buried in the mess, pareidolia, hidden face
```

實測有效，v2 完全乾淨。

### 3. 產線兩顆地雷（都是我自己埋的，已修）

| 我做的「改善」 | 造成的災難 |
|---|---|
| 加文案格式防呆 | **19 支排程有 11 支被擋**。錯在用 `startswith` 看開頭，但合格寫法常是「An entire bag of flour has exploded... and **Taco** is...」，主詞在句中 |
| 加 `LEAD_HOURS=16` | 第 267 行的 `return` 讓產線在「等發布」的 16 小時內每 20 分鐘醒來就收工，**下一支永遠不會開始生** |

兩個單獨看都是改善，合在一起就是產線全面停擺。都修了，回歸測試確認沒退步。
**這就是「修掉一個 bug 卻增加更多 bug」的實例，留著當教訓。**

### 4. 本機生圖七輪實測

詳見 `本機生圖-實測報告.md`。核心結論：

- **IP-Adapter 管得住「角色長什麼樣」，管不住「畫面有幾個角色、誰站哪」** → 構圖只能用 ControlNet
- 不要在同一個 workflow 現抽深度圖（16GB RAM 會 `Fatal Python error`）
- 本機生圖**鎖不住角色細節**（黑點眉），今天再次驗證

### 5. 今天真正走通的路線 ⭐

```
Gemini 生場景圖（免費、細節強、黑點眉保得住）
  → 本機 Wan 2.2 兩段接龍 i2v（不限量、不用瀏覽器）
  → 自製音效 21 事件 → −14.7 LUFS
```

**關鍵洞察**：i2v 只負責讓畫面動，麵粉／腳印／黑點眉全部來自起始圖，
模型不用自己生 —— 正好避開本機最弱的細節生成。
這也是為什麼紅酒片一直失敗（那支的梗就在細節本身），麵粉片一次就成。

**接龍第二段沒崩**，這是三次實測以來第一次。關鍵是 prompt 明寫
`husky stays fast asleep, never moving`。

### 6. 新增的東西

| 檔案 | 做什麼 |
|---|---|
| `.claude\skills\local-genmedia\` | 技能包：本機生成媒體管線（技術美術＋應用研究員＋MLOps 三合一） |
| `.claude\G-Brain\02-環境\可複用的工具.md` | 16 支跨專案可複用腳本的清單 |
| `.claude\CLAUDE.md` | 加了 G-Brain 的讀寫觸發規則（原本完全沒提到，所以兩個視窗各踩同一個坑） |
| `auto\make_depth.py` | 深度圖抽一次存檔 |
| `auto\clean_depth.py` | 抹平深度圖地板區域 |
| `auto\gen_scene_local.py` | 本機生場景圖（IP-Adapter ＋ ControlNet） |
| `pipeline.py` 的 fallback | **Gemini 生圖失敗 → 自動改用本機**，不再卡住等下一輪 |

---

## 待辦（依優先序）

1. **D4S1 換無品牌場景圖 → 發布**（見最上面）
2. 8 支排程文案第 2 條是第一人稱，會被格式檢查擋下（Day 13–19，還很遠）
3. `pipeline.py review` 顯示的影片路徑用固定慣例組，跟 `state.json` 實際路徑不一致（只是顯示問題，發布不受影響）
4. `sweep()` 呼叫 `build()` 時漏傳 `depth`／`cn_strength` → 掃出來的參數是在「沒有 ControlNet」條件下選的
5. 那支 65 觀看的雙胞胎還在跟 9,882 那支互吃流量 —— 但**轉不公開違反你自己的鐵律 #3**（不下架不刪除），要動請你決定

## 下一支拍片前務必套用

- 粉塵／粉末寫死 `pure white`，並禁止 `no smoke, no grey dust, nothing rising from behind the furniture`
  （這支的粉塵被生成灰褐色、還從沙發後面升起，是首幀最大失分點，而且**後製救不回來**）
- 白狗不要演「被白色覆蓋」，物理上演不出來。要弄髒 Taco 請用可可粉、泥巴、深色顏料
- 商品包裝一律 `plain unbranded`（已寫進 3 支的 scenePrompt）
