# 接手筆記 — 2026-08-23 05:30（🔴 產線的負面提示詞從來沒有生效過）

> 本檔由新到舊。開工先讀這段。**這條可能是今年最重要的一個發現。**

## 鐵證

`ComfyUI\comfy\samplers.py:610`：
```python
if math.isclose(cond_scale, 1.0) and model_options.get("disable_cfg1_optimization", False) == False:
    uncond_ = None          # ← cfg=1 時，負面那條分支直接不算
```
而 `auto\make_video_local_5s.py:290` 是 `"cfg": 1.0`。

**實測**（SDXL 快驗，同 seed 909090、其餘參數完全相同，只換負面詞）：

| cfg | 餵兩組**完全相反**的負面詞 | 結果 |
|---|---|---|
| 1.0 | 「bad hands, deformed, blurry」vs「beautiful, masterpiece, cinematic」 | **逐像素完全相同，最大差 0** |
| 2.5 | 同上兩組 | 平均差 16.0、最大差 229 |

→ **產線 NEG 欄那一長串從來沒被計算過一次**：
`two huskies, three dogs, multiple dogs, duplicate dog, extra dog, cloned animal,
extra limbs, extra legs, deformed…`

## 這解釋了什麼

- 哈士奇為什麼一直增生成兩隻（`two huskies` 有寫，但沒跑）
- 為什麼會長第五肢（`extra limbs` 有寫，但沒跑）
- memory 那條「負面詞完全擋不住，只能寫進正面 prompt」—— **不是模型不聽話，是程式碼被跳過**
  （已更正 [[local-video-model-prop-drift]]）

## 補救：兩條路

1. **NAG 節點（零安裝，本機已有）**：`comfy_extras\nodes_nag.py` 的 `NAGuidance`，
   第 85 行 `m.disable_model_cfg1_optimization()` 就是把分支強制算回來。API 已註冊可直接用。
   ⚠️ 代價：推論時間約翻倍；節點自標 `is_experimental=True`。第一次用一定要 A/B 驗。
2. **換非蒸餾模型（正規解）**：`QuantStack/Wan2.2-TI2V-5B-GGUF` Q4_K_M 約 3.43 GB，
   跟現在的 turbo（3.44 GB）同級、VRAM 不變，但 cfg 3.5~5 與步數 20~30 全部拿回來。
   ⚠️ 代價：慢 5~7 倍。

**建議順序**：先掛 NAG 做 A/B（零成本），有效再考慮換模型。

## 同一份調查驗證過的其他事實

- ✅ `QuantStack/Wan2.2-Fun-5B-Control-GGUF` **確實存在**（apache-2.0、Q4_K_M 與現有 turbo 同級大小）。
  它同時吃 `ref_image`（鎖角色外觀）＋ `control_video`（逐幀鎖動作）——
  **動作與外觀拆成兩條獨立的線**，正是「鎖死 1.29 vs 放開 6.88」那個二選一的解。
  節點 `Wan22FunControlToVideo` 是 ComfyUI 0.30.0 **內建**，不用裝自訂節點。
- ✅ **DA3 深度模型已在硬碟**：`models\geometry_estimation\depth_anything_3_base.safetensors`（541 MB），
  節點 `nodes_depth_anything_3.py` 也內建 → 「真狗影片逐幀轉深度圖當 control_video」零下載可做。
- ✅ **umt5 是本機最大常駐物件**：`umt5_xxl_fp8_e4m3fn_scaled.safetensors` 實測 **6.27 GB**，
  比 Wan 本體（3.44 GB）大快一倍。city96 有 GGUF 量化版，Q5_K_M 約省 2 GB 以上。
- ✅ **8/8 就生過 704×1280 的成品**（`output\taco_bedhq*_00001_.mp4`），但現在腳本仍是 480×832。
  Wan VAE 壓 32 倍 → 480×832 內部只剩 15×26 格，幾像素的黑點眉沒有格子可放，
  消失是結構性的。704×1280 有 22×40。**值得把兩者並排比對一次**。
- ⚠️ 研究提到「RAM 只剩 2.3 GB」是誤導：那是 ComfyUI 開著時的值（它自己佔 5.15 GB），
  關掉後回到 7~8 GB。不要拿 2.3 GB 去否決 6 GB 的方案。

---
---
# 接手筆記 — 2026-08-23 02:00（ControlNet 實測：depth 可以鎖狗的構圖與姿勢）

> 本檔由新到舊。開工先讀這段。

## 🔴 這是解 D15／D19 那類問題的鑰匙

在 `ai-video-local` 那邊實測本機既有的 `xinsir_controlnet_union_sdxl_promax`（2.5GB），
完整報告在 `ai-video-local\workflows\姿態控制實測報告.md`。對狗片最重要的一條：

**depth 模式在 strength 0.8 就完美生效，而且完全不破壞畫風。**

拿現成的 `ComfyUI\input\d9s1_depth_clean.png`（小狗在前、哈士奇在右後、沙發在左後）
當控制輸入，生出來的圖：三者位置**全部對應深度圖**，兩隻狗都沒崩、四肢關節正常、
藍項圈與犬種正確、畫面是寫實照片風格。

對照組 openpose：要 strength ≥1.2 才跟隨骨架，而且一跟隨畫風就崩成 Q 版娃娃
（因為 openpose 會連頭身比一起強制，depth 只給空間結構不管解剖比例）。

### 這解了什麼

| 卡了很久的問題 | depth 怎麼解 |
|---|---|
| D15 哈士奇一直增生成兩隻（5 張全滅） | 深度圖直接指定「這裡一隻狗」，不靠 prompt 求 |
| D19 沙發缺口畫不出來（3 張全滅） | 深度圖直接畫出缺口的形狀 |
| 四足動物沒有 OpenPose 可用 | depth 是目前唯一能鎖狗姿勢的方式 |
| 角色一致性 | `taco_V1_use035` 也是 SDXL LoRA，**可以跟這顆 ControlNet 掛同一條管線** |

⚠️ **但代價要先知道**：這條路是 SDXL，語意理解比 FLUX 弱一級
（實測 FLUX 畫得出「兩人遞飲料」的互動，SDXL 變成兩人各拿一罐）。
D16 那種「狗站蜂蜜湖中央＋倒下的罐子＋睡著的哈士奇」的複雜構圖，SDXL 可能吃不下來。
建議的用法不是全面換掉 FLUX，而是**專門用在 FLUX 畫不出來的那幾支**
（要指定物件位置、要殘缺物體、要特定姿勢的）。

### 怎麼跑

```
cd "C:\Users\TUF Gaming\ai-video-local"
python workflows\sdxl_pose_control_test.py --subject dog ^
    --pose d9s1_depth_clean.png --union-type depth ^
    --width 768 --height 1152 --strength 0.8 --steps 10 --cfg 2.5
```
深度圖用 `auto\make_depth.py <input圖> <輸出名>` 先抽好存 PNG
（⚠️ 不要把深度模型跟 SDXL＋ControlNet 塞同一個 workflow，這台機器會 Fatal Python error）。

### 順帶確認的兩件事

- **本機沒有任何 FLUX ControlNet**，而且 XLabs 的 FLUX 集合根本沒有 OpenPose、
  InstantX Union 只標 dev。要走 FLUX 姿態控制得下 6.6GB＋FLUX dev 本體，RAM 撐不住。
- `sd_xl_base_1.0.safetensors` **目前是壞的**：25 步 cfg7 生出純雜訊且只花 32 秒，
  log 無報錯。要用非蒸餾 SDXL 先試 `RealVisXL_V5.0_Lightning_fp16`。

---
---
# 接手筆記 — 2026-08-23 00:30（動作實驗：運動量不是做不到，是被 prompt 鎖住的）

> 本檔由新到舊。開工先讀這段。

## 🔴 這輪推翻了一個用了很久的假設

原本以為「動漫片動得起來是因為 2D 容錯率高，寫實狗片動大就崩」。
**錯了。用同一張靜態起始圖，只改 video prompt 的寫法，狗片運動量從 1.29 → 6.88（5.3 倍），
還超過動漫武士少女本身的 6.07。**

| 版本 | 運動量中位數 | 說明 |
|---|--:|---|
| A 現行寫法（塞滿 never／stays／keeps exactly 的禁令） | 1.29 | 就是這個把模型鎖死的 |
| B 動漫式寫法（直接描述要什麼動作，零禁令） | **6.88** | 同一張起始圖、同一個模型 |
| 動漫武士少女（整支） | 6.07 | |
| D4 麵粉（已破萬 22,092） | 3.15 | |

實驗檔：`auto\clips\_expB.mp4`、prompt 在 `auto\clips\_expB_video.txt`。

## ⚠️ 但角色崩了——所以禁令句是必要之惡，不是多餘的

B 版的畫面：狗真的站起來、抬頭、蜂蜜噴濺到空中，動作幅度完全夠。
**代價是身體整個拉長變成中型犬比例（不是吉娃娃）、多出一條線狀的第五肢、黑點眉消失。**

→ 所以現在的取捨是明確的兩端：
   **鎖死＝不崩但像靜態圖（1.29）／放開＝動得比動漫片還多但角色崩（6.88）**

## 🔴 中間地帶在哪：這才是該做的下一個實驗

動漫片能「又動又不崩」不是因為 2D，是因為它的角色一致性有別的東西在撐。
狗片要拿到中間地帶，兩個方向：

1. **部分放開**：只放開頭部與尾巴、鎖住四肢與體型（禁令句只留 body/legs 那幾條）。成本低，可以先試。
2. **訓 Wan 版 Taco LoRA**（根治）。讀檔頭確認過現有的 taco_V1_use035 是 SDXL LoRA，
   掛不上 Wan；但同目錄的 nubobear 是 Wan LoRA，證明可以訓。
   LoRA 在生影片全程持續鎖角色特徵，才有本錢放開動作。

## 補充：起始圖動勢也是一個獨立因素（動漫片內部對照）

同一支動漫片、同一畫風、同一模型，五個鏡頭的運動量跟起始圖姿勢強相關：

| 鏡頭 | 起始圖姿勢 | 運動量 |
|---|---|--:|
| s3 拔刀 | 有動勢 | 11.72 |
| s4 揮刀 | 強動勢 | 9.12 |
| s2 睜眼特寫 | 靜態 | 6.58 |
| s1 站著背對 | 靜態 | 4.44 |
| s5 收刀站定 | 靜態 | 3.61 |

起始圖有動勢的平均 10.42、靜態的 4.88，**差 2.14 倍**。
→ 兩個因素是疊加的：prompt 寫法（5.3 倍）＋ 起始圖動勢（2.14 倍）。
   狗片目前兩個都沒吃到。

---
---
# 接手筆記 — 2026-08-23 00:10（音樂庫補齊＋角色聖經失效盤點）

> 本檔由新到舊。開工先讀這段。

## 音樂庫：2 首 → 6 首，剪裁已腳本化

賢賢問「動漫女孩自己去 PeriTune 拉音樂，為什麼你這邊做不到」——做得到，之前是把它
當成裁示項丟回去問，錯了。已補：

- `sfxgm原始\` 六首可商用曲＋`授權與出處.md`（含實測頻譜值與再抓一次的指令）
- `sfx\make_bgm_clip.py`：`python sfx\make_bgm_clip.py cheer-up 12.3 --start 8`
  → 產出 `_prerenderedgm_cheer_up_12.3s.wav` 並印出配方引用行。已預剪兩首備用。
- **Chosic 擋 WebFetch（403）但不擋帶 UA 的 curl**；分類頁的 mp3 是 JS 動態載入的，
  HTML 裡只有 `download-audio/<id>` 連結，要再抓一層曲目頁才有直連。

**⛔ Kevin MacLeod 的曲子不要用**（Quirky Dog／Scheming Weasel 曲風其實很對狗片）：
incompetech 官網明講音樂已主動註冊進 Content ID，會收到 claim、72 小時才解除。
檔案留在 `bgm原始\_有ContentID風險\` 只為記錄「查過了、結論不要用」。

**實證了 PUBLISH_GATE 規則 12 的「選曲靠量測不憑曲名」**：Sneaky Snitch 名字最對
「偷偷幹壞事」，量下去低頻 58.8%＝潛行陰謀的音色，不是歡樂闖禍。判準是
低頻 <30%、中頻 >50%、起音密度 >4.4/秒。

## 🔴 角色聖經（`character\角色設定.md`）目前是失效的

賢賢問「不是有角色聖經嗎」——有，但三件事都壞了：

1. **沒有任何程式讀它**（`grep -rn "角色設定" --include="*.py"` 零命中）。
   角色描述是整段複製在 schedule.json 裡，19 支有 17 支各存一份。改角色要改 17 個地方。
2. **它自己寫的核心機制被丟掉了**：文件裡寫「每次生成都附 `character/taco-ref.jpg`
   定裝照＋角色描述句一字不改」。現在的產線只餵文字、不餵定裝照，
   `character\` 裡 8 張定裝照沒有任何一張被用到。
3. **內容過期**：頂部 8/6 改版說明寫 Taco 是全白＋黑點眉，
   下面「舊版設定」卻還寫著「左耳一塊焦糖色斑（頻道辨識物，每支都要看得到）」。

## 🔴 LoRA：親自驗過檔頭，掛不上 FLUX 也掛不上 Wan

不是引用舊結論，是讀 safetensors 檔頭確認的：

| LoRA | 張量 key 樣本 | 底模 |
|---|---|---|
| `taco_V1_use035` | `lora_unet_down_blocks_1_attentions_...` | **SD/SDXL UNet** |
| `nova_V1_use035`／`nova_lora_V1/V2` | 同上 | SD/SDXL UNet |
| `nubobear_10_steps`（別的專案） | `diffusion_model.input_blocks.0.0.alpha` | **Wan / DiT 系** |

→ Taco/Nova LoRA 只能配 SDXL 生圖。但 SDXL 的語意理解比 FLUX 差一級
（動漫實測：FLUX 畫得出「兩人遞飲料」的互動，SDXL 變成兩人各拿一罐），
D16 那種「狗站蜂蜜湖中央＋罐子倒在旁邊＋哈士奇睡沙發」的構圖大概率畫不出來。
→ 而且 LoRA 只作用在**生圖**階段，救不了 Wan 生影片時把黑點眉洗掉的衰減。
→ **nubobear 證明 Wan LoRA 是可以訓的**。要根治黑點眉，該訓的是 Wan 版的 Taco LoRA，
   不是繼續用 SDXL 版。這是目前最值得排的一個實驗。

---
---
# 接手筆記 — 2026-08-22 23:20（D16 出片＋運動量閘門實測，這段最新）

> 本檔由新到舊。開工先讀這段，再往回看。

## 一句話狀態

**D16S1 蜂蜜片做完了，score_video 11/12 過、1 項警示，在 `待審核\` 等賢賢裁決。**
原訂要拍的 D19 因為 FLUX 畫不出殘缺家具而換掉。這輪最有價值的產出不是片，
是把 8/22 新立的運動量閘門推到底測出來的三件事。

報告頁（含全部數字與來源）：https://claude.ai/code/artifact/1ecb3586-abec-428b-85d4-fea60547ac87

## 🔴 運動量：三件實測結論

1. **把環境運動寫進 videoPrompt 完全無效。** 這輪原本的假設是「讓蜂蜜持續流動、
   灰塵飄」就能繞開靜態問題。實測 S1 段 **0.0050**，跟 D13S1 的 0.0052 一模一樣 ——
   一個數字都沒動。假設被自己的量測推翻。
2. **把頭部動作寫密才有效，+80%。** 同一支片、同一起始圖、同一次接龍的 S2 段改成
   「低頭→抬頭→左歪→右歪→轉開→轉回，中間穿插眨眼」，量到 **0.0090**。
   還是不到門檻 0.0100，但這是模型真的在動，不是後製。
3. **兩把尺量的是不同東西。** 同一支第一版組裝片，灰階幀差量到 0.0144、
   score_video 只認 0.0068。score_video 用 ffmpeg 的 scene_score（畫面內容變化），
   **對純縮放幾乎沒反應、對平移才有反應**。想過這道閘門，光推鏡沒用，一定要有平移。

## ⚠️ zoompan 兩個坑（都量出來了，寫在 `auto\_build_d16.py` 註解裡）

- **`d=1` 時 `zoom` 變數每幀重置**，累積要用輸出幀號 `on`。
  寫 `z='min(zoom+K,…)'` 量出來會跟原片一模一樣（三種強度都 0.0051）＝ 根本沒生效。
- **平移撞到畫面邊界就等於停住。** x 上限是 `iw − iw/zoom`，越快越早撞，撞完的幀完全不動。
  有一版全片 0.0097（差門檻 0.0001），逐鏡拆開才發現最長那段調**強**反而變**低**——
  2.8px/幀只要 40 幀就撞界，後面 57% 的幀是死的。
  正解：`px ≈ (iw−iw/zoom)/2 ÷ 該段幀數`，全程剛好用完。

## 🔴 D19 換掉的理由：FLUX 畫不出「被破壞的家具」

「黑洞吃掉半張沙發」抽三張場景圖，**三張的沙發都完好無缺**。prompt 一次比一次具體
（從「缺一角」寫到「左半邊整個不見、看得穿到後面牆壁、切面有黃色泡棉掉出來」），
沒有一次生得出來。跟 D15「一整盤麵倒在地上」同類：**訓練資料裡的家具都是完整的**。
三次不通就換路，沒抽第四張。候選圖留在 `clips\d19s1_scene_v1沙發沒缺口.png` 等三張。

→ 改拍 D16 有數據支撐：`material_blocklist` 直接寫著「D16 蜂蜜是真實材質，必須放行」，
   而蜂蜜攤地跟已破萬的淹水（31,847）、麵粉（22,092）同類。

## ⚠️ 兩道閘門互相矛盾（需要裁示）

第一版 videoPrompt 加運動描述後被 plan_model 判 **BLOCKED**（命中「小幅度動作」，
本機 1 分、12 支全滅）；但把動作拿掉之後，生出來的片又過不了運動量閘門。
**守門要你繞開動作，運動量閘門要求你有動作。**
這輪的走法是拿掉所有有實測失敗紀錄的動作（尾巴搖、耳朵抽動、靠墊掉落），
只留 8 分的頭部動作並寫密，缺口用剪輯運鏡補。這是繞路，不是解法。

## 逐幀補眉：第二次失敗，這條路建議放棄

`_paint_dots_video.py` 在這支片一樣失敗：121 幀裡偵測失敗 41 幀，補出來的點會閃爍、
左右不對稱、時有時無，**比不補更糟**。D10 那次的解釋是「側頭偵測不到眼睛」，
但這支狗**全程正面朝鏡頭仍然偵測不穩** —— 問題比原本認定的更基本。
已改用場景圖補繪版，並把 INS2 臉部特寫排在 6.8–8.3 秒，在招牌消失前再交代一次。

## 動過的檔

- 新增 `auto\_build_d16.py`、`sfx\mix_d16s1.py`、`auto\clips\d16s1_*`
- 新增音效 `sfx\lib\_prerendered\d16_*.wav` 五個（全部避開 D13 用過的原始檔）
- 新增素材 `EFX INT Dog Wimper 26`、`EFX SD Pig 02 Snort 18`、兩個新 whoosh
- `auto\model_policy.json`：物件互動加一筆誤傷紀錄（`smear` 當名詞用會被擋，
  跟 press 誤中 unimpressed 同型；動詞 smear 該擋所以沒動 keyword，改在劇本端換字）。
  19 支回歸測試零誤傷。

## 等賢賢裁示（這輪新增，舊的仍未清）

1. **D16 發不發**（缺陷三個：黑點眉撐到第 8 秒、玻璃罐後段慢慢變形、蜂蜜顏色偏橘像果汁）
2. **運動量靠鏡頭運動達標算不算數** —— 要不要把閘門改成分開量「主體運動」與「鏡頭運動」
3. **D19 黑洞吃沙發怎麼辦**（改吃別的／換題材／擱著）
4. **哈士奇「清醒見證者」要不要全部改成「睡著」** —— D16 我照 D13 驗證過的配置改了
   （標題笑點罪證都沒動，只有配角狀態改），D17／D18／D19 是同一個寫法
5. **能力表「連續劇情＝接龍必崩」已被 8/21 推翻但沒更新** —— 守門還在用被推翻的數據判事
   （這支片的接縫逐像素差 2.52，跟 8/21 那次的 2.68 一致）
6. **BGM 只剩兩首在輪**，音效庫有一萬個音效但音樂一首都沒有

---
---
# 接手筆記 — 2026-08-21 01:35（賢賢一次核准 8 項，本輪執行結果）

> 本檔由新到舊。開工先讀這段，再往回看。

## 一句話狀態

**D12S1 已經發布上線了**（頻道第 14 支），D10 從三輪失敗救回來、素材與組裝都完成，
接龍的能力邊界也問清楚了。剩 D10 的音效與逐幀 gate、D13/D15 的拍攝沒做完。

## 8 項的實際結果

| # | 賢賢核准的事 | 結果 |
|---|---|---|
| 1 | D12S1 發布 | ✅ **已公開**：https://youtube.com/shorts/dMJavbkX6C8 |
| 2 | D10 救回來 | ✅ 單狗版畫面全過、`d10s1-cut.mp4` 12.04 秒；**音效與 gate 未做** |
| 3 | 選題材質硬規則 | ✅ 上線，回歸測試零誤傷 |
| 4 | 每日自動追蹤 | ✅ 排程 `TacoNova-TrackStats` 每天 09:00 |
| 5 | 下載 Fun-5B-InP | ✅ 照建議不下載 |
| 6 | 加記憶體 | ✅ 沒動（要花錢，也不是我能執行的） |
| 7 | 接龍進產線 | ✅ 實驗做完了，**結論是可以接，但有條件**（見下） |
| 8 | D13／D15 開拍 | 🔶 **D13 的 S1 拍完了**（哈士奇沒崩、黑點眉撐住）；D15 場景圖抽了 5 張還沒選到能用的 |

## 🔴 接龍的能力邊界（這輪最重要的發現）

第一輪實驗我又挑錯動作（低頭→抬頭本來就不會崩），跟 8/20 那次犯同一個錯，作廢。
第二輪改用死穴動作「抬起單邊前腳」，同起始畫面同 seed，才測出東西：

| | 單張圖 | 接龍 17 格 |
|---|---|---|
| 身體比例 | ❌ 整個拉伸變形 | ✅ 正常 |
| 前腿 | ❌ 變得極長、指向錯誤 | ✅ 站立的三隻腳正常 |
| 多餘肢體 | ❌ 有來路不明的爪子 | ✅ 沒有 |
| **抬起來的那隻腳** | ❌ 畫壞 | ❌ **一樣畫壞**（腳掌比頭還大） |

**接龍鎖的是「身分與連續性」，不是「動作能力」。**
→ 可以接進產線：它確實擋住了 8/9 判定「本機不能接龍」的那些症狀（長人手／增生肢體／身體拉伸）
→ 但不要拿它去解本機原本就做不到的動作，天花板沒有被抬高
（原始數據與逐幀觀察在 `auto\_exp_20260821_接龍換動作\實驗說明.md`）

## D10 是怎麼救回來的

**三輪失敗的根因不是題材，是哈士奇同框。** 改成單狗之後一次就過：
單狗全程、四肢關節清楚、場景零漂移。劇本改動：
Nova 完全不入鏡、標題從「藏到棉被下」（拍不出來的動作）改成裝無辜、
scenePrompt 尾巴那段從麵粉片複製殘留的「paper flour bag」清掉。

組裝 `_build_d10_v2.py`（照 D12S1 節奏）：S1a / INS1 蛋盒 / S1b / INS2 腳掌 / S2 接龍主鏡。
S2 從 0.71 秒起跳切掉接龍的還原 17 格，色差被前面的插入鏡隔開。

**還沒做**：音效（`sfx\mix_d10s1.py` 是雙狗時代的舊配方要重寫，
而且 BGM 不能跟上一支 D12 的 apple-cider 重複 —— 用 happyclappy）、
逐幀 gate 憑證、finish_video。

## 🔴 產線的三個真 bug（都修掉了，但要知道）

1. **`publish_video.py` 驗證查錯分頁** —— 它只查 Studio 的「影片」分頁，
   但這頻道發的全是 60 秒內直式片，YouTube 一律歸到 **Shorts 分頁**，
   「影片」分頁永遠是空的。所以發成功也會被判失敗。已改成兩個分頁都查。
2. **「發布」鍵在沒選公開時叫「儲存」** —— 舊碼只看 `click_exact` 有沒有回 True，
   PUBLIC 沒真的選中時它撈到別的東西也回 True，影片就停在草稿。
   已改成看 `aria-checked`。**D12S1 就是這樣卡住的，最後靠手動走完 wizard 才發出去。**
3. **FLUX 生不出黑點眉** —— 白燒一次 6.3 分鐘。
   場景圖生完**一定要先補繪再生影片**：`python auto\_fix_d10_dots_v3.py <場景圖> <x1,y1> <x2,y2> [R]`
   座標要自己畫格線量（量法：裁頭部 → 放大 → 疊格線 → 讀眼睛中心，眉在眼正上方約 0.42 倍眼距）

## ⚠️ 逐幀補眉這條路目前不好用

`_paint_dots_video.py` 在 D10 上失敗：狗抬頭側望時偵測不到眼睛，
黑點被畫到耳朵和臉頰上。已加 `--miss-tolerance`（連續失敗超過 N 幀就停手不畫），
但**根因是側頭時的眉位算法本身會算錯**，不是沿用策略的問題。
D10 最後改用場景圖補眉版（f0 清楚、後段被 Wan 衰減）。

## D13 場景圖踩到的兩件事

1. **FLUX 不理會 `BACKGROUND, slightly out of focus`** —— 第一版把哈士奇畫成右下角大特寫，
   身體還有斷裂感，正是雙狗崩壞的高風險構圖。改成明確的量化約束才有效：
   `FAR BACKGROUND, small in the frame, no closer to the camera than the sofa,
   never larger than the chihuahua in the frame`。第一版留在 `d13s1_scene_v1哈士奇太大.jpg`。
2. **項圈被畫成綠松色不是藍色**（Taco 的招牌是藍項圈）。這次沒重生，先接受。

## D13 拍出來的結果（好消息）

`auto\clips\d13s1.mp4`（5.04 秒 S1 原片）：
**哈士奇在背景睡著、完全沒崩**，黑點眉四幀都還在（比 D10 撐得久）。
→ 「量化約束」這招對 FLUX 有效，值得沿用到之後每一支雙狗片。
結尾一樣是轉身側望（Wan 對 `turns his head away` 的固定詮釋，D10 也一樣）。

## D15 卡在場景圖（未完成）

抽了 5 張都不能用，候選留在 `clips\d15s1_scene_v*.jpg`：
- v1：**右邊增生成兩隻哈士奇**
- v2：只剩一隻哈士奇 ✅ 但**吉娃娃臉部扭曲**（皺眉瞇眼、嘴部變形，不是 Taco 的臉）
- v3：又是兩隻哈士奇（沙發上一隻、地上一隻）
- v4：吉娃娃臉正常 ✅ 麵條掛嘴上 ✅ 藍項圈 ✅，但右邊一樣多一隻（哈士奇＋一隻米色的狗）
- v5：兩隻哈士奇，而且吉娃娃整個埋進麵裡看不到臉

**5 張全中同一個病：哈士奇增生成兩隻。** 這不是多抽幾張能解的 ——
D13 用完全相同的量化約束卻穩定只有一隻，所以問題出在 D15 這支 prompt 的結構本身
（推測是 husky 在句子裡出現的次數與位置，未驗證）。

**建議：D15 直接改單狗**（哈士奇完全不入鏡），照 D10 的做法 —— 那是目前唯一穩定的解。
標題「Taco Got Caught With One Noodle Still In His Mouth」不需要哈士奇也成立。
這是劇本改動，等賢賢點頭。

順帶：麵條那盤一直畫不出「一整盤倒在地上」，只有醬和一顆肉丸。
選到能用的之後，記得**先補黑點眉再生影片**。

## 等賢賢裁示（新的）

1. **D16／D18 的哈士奇也是「清醒的不以為然見證者」**，跟 D13/D15 原本一樣是崩壞高風險。
   要不要比照改成「背景睡著不動」？（我沒擅自動，那超出你核准的第 8 項範圍）
2. **D17「把 Nova 埋在爆米花裡」題材本身就要哈士奇大面積入鏡**，這支要不要換題材？
3. **產線要不要加一道「場景圖沒補黑點眉就不准生影片」的閘門？**
   代價是排程不能全自動（每支都要人工補繪座標）。
4. **D10 的腳掌是乾淨的**（模型沒畫沾黃），跟 D12S1 腳掌太乾淨是同型物理矛盾，接受嗎？
5. **D13 的項圈是綠松色不是藍色**，要不要重抽場景圖直到藍色為止？
6. **D15 要不要直接改單狗？**（5 張場景圖全部增生成兩隻哈士奇，改單狗是目前唯一穩定的解）

## 沒做完的

- D10 音效 + 逐幀 gate + finish_video
- D13 影片（場景圖在生）、D15 全部
- pipeline 的 20 分鐘 tick 排程**還沒重新啟用**（`TacoNova-Pipeline` 目前 Disabled，
  而且重複期限 192 小時早在 8/16 就過期了）。要恢復自動產線得先修那個任務。

---
---
# 接手筆記 — 2026-08-21 00:15（多幀接龍實測，這段最新）

> 本檔由新到舊。開工先讀這段，再往回看。

## 一句話狀態

**多幀接龍成立，9.375 秒的單一連續主鏡已經做出來了**（對標目標 9.57 秒）。
產線只動 `auto\make_video_local_5s.py` 一個檔，加 `--continue` / `--anchor` / `--join`，
舊用法（單張場景圖）行為完全沒變，pipeline 不用改。

## 怎麼用

    # 第一段：照舊
    python auto\make_video_local_5s.py 場景.jpg prompt.txt clips\d16s1.mp4

    # 第二段接上去，順便接成長鏡
    python auto\make_video_local_5s.py - prompt2.txt clips\d16s1_s2.mp4 ^
        --continue clips\d16s1.mp4 --join clips\d16s1_長鏡.mp4

每支片現在會多產一個 `.raw704.mp4`（ComfyUI 原生 704x1280）。**接龍一定餵這個**，
不要餵 1080x1920 成品（成品放大過又裁掉 44px）。給成品路徑時程式會自動改用旁邊的原生檔。

## 量出來的數字（都是這台跑的，不是網路上的通論）

| 項目 | 數字 |
|---|---|
| 生成時間 | 6.3 分鐘（單張是 6.5–7.8 分鐘，**接龍沒有比較慢**） |
| 同一段內相鄰幀的正常跳動 | 1.58 |
| ✅ 正確接法的接縫 | **2.68**（正常跳動的 1.7 倍，看不出來） |
| ❌ 錯誤接法的接縫 | 8.58 |
| 舊做法（兩段各生再硬接） | **24.56**（正常跳動的 15.5 倍） |
| 錨點抽取準確度 | 逐像素平均絕對差 **0.000**（crf 0 無損切） |
| 接龍段內亮度漂移 | 頭 171.51 → 尾 174.19（+2.69，很輕微） |

**接縫比舊做法小 89%。**

## 🔴 接法：砍前一段的尾巴，不要砍新這段的頭

這是量出來才發現的，直覺會做反：

    ✅ 前段[0 : 總格數−17] ＋ 新段整段          接縫 2.68
    ❌ 前段整段 ＋ 新段[17:]                     接縫 8.58

因為新這段的前 17 格是「餵進去那 17 格被 VAE 還原回來的版本」，跟原檔差一個來回誤差
（RGB 平均位移 −1.1、逐像素差 2.31）。接縫要挑在同一次解碼的內部，兩邊才會是同一個色域。
`--join` 已經照 ✅ 寫死，不用自己算格數。

## 三件被這輪推翻／確認的事

1. **「本機不能接龍」（8/9 的結論）→ 推翻。** 那次崩掉是因為只餵一張圖 ＝ 零運動資訊、
   零身分錨。ComfyUI 0.30.0 的 `Wan22ImageToVideoLatent` 本來就吃多幀
   （`start_image[:length]` → encode → `mask[:,:,:latent_temp.shape[-3]] *= 0.0`
   ＝ **餵幾格就鎖幾格 latent，KSampler 完全不動**）。
2. **研究列的最大風險「5B TI2V 訓練時是單幀條件，可能不吃多幀」→ 不成立。** 實跑沒拒絕、
   沒崩、沒變慢。
3. **P0 的做法要改。** 研究說「P0 追的不是拉長單段，是把接縫藏好（遮擋藏刀）」——
   現在接縫本身就看不出來，**不需要藏刀**，直接接就是一鏡到底。

## ⚠️ 這輪沒有證明的事（不要過度推論）

**沒有證明「多幀接龍讓角色不會崩」。** 對照組（D12 現有那支 S2）本來就沒崩 ——
D12 是靜態表演鏡，只動頭和眼睛，本來就是本機最穩的題型。
要證明接龍真的擋得住「長人手／增生第二隻狗」，得拿一個**動作會換**的第二拍來測，
不是重演同一個表情。這是下一個該做的實驗。

## D13 / D15 判 BLOCKED 是誤報，不用改劇本

| 餵給 plan_model 的欄位 | D13 | D15 |
|---|---|---|
| `oneLine`（故事摘要） | ⛔ BLOCKED 物件互動 | ⛔ BLOCKED 拿取物品 |
| `videoPrompt`（真正會拍的） | ✅ LOCAL_WAN_TRIAL | ✅ LOCAL_WAN_TRIAL |

而 `pipeline.py:380` 餵的是 `videoPrompt`。**產線根本不會擋這兩支**，
state.json 沒有它們的 BLOCKED 紀錄，pipeline.log 從頭到尾沒出現過 BLOCKED。

原因：這兩支的 videoPrompt 在 8/18 的 d11-19 改寫時早就寫成「災難已成＋只動頭和眼睛」了
（羽毛已經炸滿地、義大利麵已經倒在地上）。BLOCKED 的是摘要那句「用爪子把羽毛塞回去」
「一根一根叼回盤子」—— 那句話不會被拍。前一輪是拿 `oneLine` 手動跑守門才得出 BLOCKED。

> 教訓：手動跑 `plan_model.py` 驗劇本時，**要餵 videoPrompt，不要餵 oneLine**，
> 否則會擋掉一堆其實拍得出來的片。

## P1（AI Upscale）建議降級，但不要刪

**「畫質提升 → 有流量」這條因果目前零證據，而 P1 是四項零成本以外唯一要下載的。**

- 破萬 5 支裡 3 支是本機生的 → 本機畫質已經夠進推薦
- 巧克力 157 畫面漂亮，旁邊麵粉 22,090 畫質相當 → 差 140 倍
- 8/8 同一天兩支標題題材長度全一樣 → 15,865 對 225，差 70 倍
- 稽核自己量的：兩支 score_video 都 10/10，重傷在動作那 20%，upscale 補的是銳度不是動作
- **沒有 Studio 登入就量不到完播率 ＝ P1 做完也驗收不了有沒有效**

不刪掉的理由（這段是推的）：稽核量到「真實紋理 FFT 中頻只有對標 0.5x」，跟「畫得出真實
材質才會爆」機制同源。但 upscale 只能讓既有紋理更銳，不會把假的發光變成真的發光。

真正該卡在前面的是**選題硬規則**（擋掉發光／透明／變身），零成本、有 8 支 5 破萬 vs
5 支 0 破萬撐著。

## 產物在哪

- `auto\clips\d12s1_s2_chain.mp4` — 接龍生的第二段（＋ `.raw704.mp4`）
- `auto\clips\d12s1_長鏡9秒.mp4` — 接好的 9.375 秒單一連續鏡（＋ `.raw704.mp4`）
- 舊版程式備份：`auto\_舊版備份\make_video_local_5s.py.before-多幀接龍-20260820`
- 這輪是實驗素材，**沒有走逐幀 gate，不能發布**

## 等賢賢裁示（累積到現在）

1. **D12S1 藍腳印發不發**（＋原有四項：標題／結尾／腳掌／項圈）
2. **D10 破蛋要不要救**
3. **要不要把「發光／透明／變身」寫成選題硬規則**擋在 plan_model 前面
4. **要不要掛排程每天自動跑 track_stats.py**
5. **要不要下載 Fun-5B-InP**（3.12 GB）—— 接龍已經拿下 P0，這顆的價值剩「雙端錨」，優先度降了
6. **要不要加記憶體到 32GB**
7. **接下來要不要把接龍接進正式產線**（改 `_build_dXX.py` 用長鏡，或叫 pipeline 自動生兩段）
8. **D13／D15 既然沒被擋，要不要直接排下去拍**（而且現在可以拍 9 秒版）

---
---
# 接手筆記 — 2026-08-20 23:45 收工（換新視窗）

> 本檔由新到舊三段。**這一段最新，先看這段。**

## 一句話狀態

**第一次把 13 支已發布片的真實播放量抓回來了，推翻了三個一直在用的判斷。**
產線程式沒動，只新增了追蹤工具。D12S1 藍腳印仍在 `待審核\` 等賢賢裁決發不發。

## 🔴 新視窗開工必讀：三個既有判斷已作廢

先跑 `python auto	rack_stats.py` 看最新數字，**不要照舊文件的結論做事**。

1. **「只有雲端 Veo 能爆」→ 假的。** 破萬 5 支裡 3 支確定是本機生的
   （淹水 31,845／麵粉 22,090／拔草 21,002）。本機不是死因，Wan 的動作能力也不是閘門。
   → 8/20 稽核那份「補強優先序 P0-P4」是建立在錯前提上的，暫時不要照做。
2. **「48 小時能判生死」→ 假的，而且錯得很貴。** 8/12 分析把 d3s1 記成「64 觀看的失敗品」，
   那支後來跑到 31,845。Shorts 會延遲綻放，要連續幾次快照都不動才算死。
3. **「抄對題材就會爆」→ 假的。** 排程全抄 Tim and Jeffy，抄的是他們 5,200 萬到 5.35 億的片，
   結果 7 支不到一千。最硬的證據：8/8 同一天發兩支**標題題材長度全一樣**，15,865 對 225，差 70 倍。

## ✅ 真正找到的規律：模型畫得出真實材質，畫不出發光／透明／變身

| 題材類型 | 支數 | 破萬 | 最高 |
|---|--:|--:|--:|
| 真實材質（水、土、麵粉、酒漬、暗洞） | 8 | 5 支 | 82,805 |
| 發光／半透明／金屬／身體變形 | 5 | **0 支** | 264 |

發光那五支（岩漿 264、傳送門 94、點石成金 55、史萊姆 50、熊貓 25）**沒有一支超過 300**。
抽幀比對圖：`比對-爆的三支對死的三支.png`（上排爆下排死）、`比對-真實材質三支.png`。
下排肉眼可見：傳送門是飄在地板上的扁平霓虹圈、史萊姆是貼紙般的粉紅圓餅、
熊貓那支狗身上長黑斑看起來像另一隻壞掉的狗。

**黑洞那支反而爆 8 萬** —— 因為黑洞在畫面上就是純黑橢圓，模型畫得出來。
所以規則不是「不能碰奇幻」，是**不能碰需要打光與透明感的東西**。

⚠️ **但畫面醜不是唯一死因，不要過度推論。** 巧克力那支畫面其實很好
（白沙發上的巧克力腳印，細節漂亮），只有 157；旁邊麵粉那支畫質相當，22,090。
兩支一樣好看差 140 倍，所以「畫面變好就會有流量」沒有證據支持。

## 剩下的排程不用改

還沒發的 8 支（破蛋、藍腳印、羽毛枕頭、義大利麵、蜂蜜、爆米花、咖啡、黑洞吃沙發）
**全部是真實材質，沒有一個發光的**。那五支發光題材剛好全集中在已發完的裡面。

## 這輪新增的東西

- `auto	rack_stats.py` — yt-dlp 免 API 抓成效，**存時間序列不存快照**（就是為了不再犯第 2 點那個錯）
- `成效追蹤.md` — 每次抓完自動重寫，有「較上次」欄
- `auto\成效追蹤.json` — 原始資料，會一直累積
- `成效儀表板.html` → Artifact：https://claude.ai/code/artifact/729116ff-7af8-44ce-8aec-b914e16a11bd
  （更新請帶 `url` 參數重發，才會保住同網址）

## 等賢賢裁決

1. **D12S1 藍腳印發不發**（真實材質、逐幀 gate 全過，躺在 `待審核\`）＋原有那四項（標題／結尾／腳掌／項圈）
2. **D10 破蛋要不要救**（真實材質，對標同題材 2,500 萬。當初放棄是因為做不出「藏」的動作，
   但按現成的「災難已成」公式，蛋已經破在地上是一張靜態圖就解決的事）
3. **要不要把「發光／透明／變身」寫成選題硬規則**擋在 plan_model 前面
4. **要不要掛排程每天自動跑 track_stats.py**

## 還沒答案

**推薦閘門到底在看什麼。** yt-dlp 抓不到流量來源。8/12 手動看 Studio 時發現 d5s1 的
「Shorts 推薦內容」只佔一小部分＝**演算法根本沒把它推進 feed**。
要再往下查只能登入 YouTube Studio 拿逐支的流量來源與前 3 秒留存 —— 那需要賢賢的帳號。

**最近 4 支（50／55／1,319／25）是死了還是還沒開**，要連跑幾天快照才知道。

---
---

# 接手筆記 — 2026-08-20 23:15 收工（換新視窗）

> 本檔有三段：18:05（產線閘門與 D12S1 現況）／22:35（對標稽核）／23:15（爬網研究）。
> **新視窗先看最後一段「23:15 這輪」，再往回看。**

## 一句話狀態

**D12S1「藍腳印」已出片、三道關全過、在 `待審核\` 等賢賢驗收**，未發布。
D10S1「藏破蛋」三輪失敗已放棄（建議換題材，賢賢尚未正式裁決）。

---

## 🔴 新視窗開工必讀：今天新增的三道閘門

### 1. 產線互斥鎖 — 一次只開一個視窗做產線
這台只有一張顯卡，並行沒收益只有衝突（8/19 夜兩視窗並行：隔壁 5 次用
`git add -A` 撿走本視窗未存的檔、一支片被擅自發布、一支複審 FAIL）。
- **開工先查**：`python auto\studio_lock.py status`
- 已接進 pipeline(tick/ok/no)、gen_scene_flux、make_video_local_5s、finish_video
- 唯讀指令不擋；卡住可 `python auto\studio_lock.py release`
- **git 沒有程式擋，是紀律**：不准 `git add -A`，一律指名檔案

### 2. 逐幀 gate — 不逐幀寫下看到什麼就不准出片
起因：賢賢抓到 D10 插入鏡裡哈士奇是一團沒關節的白肉，而那問題第二輪就抓到過、
第三輪清單也寫了要看，結果只看兩三張幀圖就宣告過關。
- `python auto\frame_gate.py <cut.mp4> --init` → 逐幀填 → `--verify` 拿憑證
- **finish_video 正式出片無憑證直接拒絕**（--dry-run 放行，方便迭代音效）
- 每幀必答三項（動物數／解剖／崩壞）+ 全片四項，空白或填「OK」會被擋

### 3. 發布必須賢賢親核
- `python auto\pipeline.py ok <key> --by-xianxian` ← 只有賢賢說「可以發」才准打
- publish() 無 `approved_by=賢賢` 一律拒發
- **改劇本遷就本機＝改成品長相＝必須先問賢賢**（D11 事故：自改熊貓→斑點狗，標題沒改）
- PUBLISH_GATE 新增第 10 條：**標題承諾的東西必須在畫面上逐項指得出來**

---

## D12S1 現況（等賢賢裁決四件事）

成片 `待審核\d12s1-有聲.mp4`（12.12s），完整說明在 `待審核\d12s1-說明.txt`。
- score_video 10/10｜preflight 全過（-18.1dB）｜逐幀 gate 24 幀全審過（pass_ccfee0319883）
- 24/24 幀單狗、四肢無崩壞、黑點眉全程兩顆對稱（首次撐過整支 Wan）

**等裁決**：
1. 標題必須改（現有標題有「擦」的動作但畫面沒有）→ 建議
   `Taco Left Blue Pawprints Everywhere And Says It Wasn't Him 🐾💙`
   **賢賢點頭才准改 schedule.json**
2. 結尾是凝視非動作中斷（素材不夠切在動作中；要修得再生一段 8 分鐘）
3. 腳掌偏乾淨但地上有藍腳印（物理矛盾）
4. 低頭段約 1.5 秒項圈滑到頸後（Wan 動態瑕疵）

---

## 這支成功的公式（下一支照抄）

1. **單狗**：Nova 不入鏡。雙狗大面積同框＝解剖必崩
2. **因果鏈塞進第一幀**：翻倒的容器 → 灑出的東西 → 痕跡 → 站在中央的犯人
3. **只用表情戲**：轉頭／低頭／抬頭。不碰 Wan 做不到的動作
4. **黑點眉**：場景圖 PIL 補繪，臉大時 R 要按眼距等比放大，羽化＝R×0.15，
   擾動係數 0.04/0.025（大半徑用 0.08 會拉成星芒），乘暗 0.06
5. 插入鏡要有資訊量（腳印／腳掌罪證），不要空鏡

## 已知待辦
- `frame_gate.py --init` 的提示訊息裡 `auto\frame_gate.py` 的 `\f` 被當跳脫字元
  （印成 `autorame_gate.py`），只是顯示問題不影響功能
- D10 素材全部保留（d10s1_*），賢賢若要復活再說

## 環境
ComfyUI 由 pipeline/restart_comfy.ps1 管理，關機即斷會自起。
財富密碼**無遠端，仍無異地備份**；AI-COMPANY 已與 origin/main 同步。


---
---

# 22:35 這輪 — 對標稽核（READ-ONLY）＋ 爬網研究

## 這輪做了什麼

**全程唯讀**：沒改、沒刪、沒裝、沒下載模型、沒跑生成、沒碰 ComfyUI / Wan / FFmpeg pipeline。
唯一寫入是這一段文字，以及暫存區的量測中間檔。

拿 D12S1 跟對標片 `oE7le11QGEM`（Tim and Jeffy 生日派對，42.8 萬觀看）做同尺規對照。

**報告（含全部圖表與數據）**：https://claude.ai/code/artifact/43aedb65-bb78-43d0-b8de-1585396cf1ef

## 三個要記住的結論

### 1. 兩支片在 score_video.py 上打成平手，都是 10/10

所以「本機做不出來」不能再用規格、片長、剪輯節奏、音量曲線解釋 —— 那些已經做到了。
**繼續打磨 prompt / 剪輯 / 混音的邊際收益已經很低**，卡住的每一項都在模型能力層。

### 2. 差距量出來的數字

| 項目 | 目標 | D12S1 |
|---|--:|--:|
| 最長主鏡 | 9.57 s | 5.00 s（硬上限 5.04） |
| 鏡內表演動能 | 5.30 | 2.83（0.53x） |
| Punchline 動能峰值 | 14.19 | 7.39 |
| 真實紋理（FFT 中頻） | 1.0 | 0.5x |

加權總分 **68 / 100**，動作那 20% 幾乎全丟，是唯一的重傷。

### 3. 補強優先序（前三項零下載）

- **P0 主鏡拉到 8 秒 —— 用剪的不是用生的**。對標片 8.40 s 那個切點就是同機位隱形切
  （構圖沒變、蛋糕憑空多一道切口）。《對標製作標準》第 3 招「遮擋藏刀」寫的就是這個。
  改 `_build_dXX.py` 的組裝邏輯即可，不用重生素材。
- **P1 AI Upscale 取代 lanczos**。`models/upscale_models` 和 `models/frame_interpolation`
  現在都是空的。收益跟模型能力完全脫鉤。**要下載，需賢賢點頭。**
- **P2 物件狀態改變 → 「災後硬切」寫劇本**。生「完好」與「被破壞」兩張場景圖各生一段硬切。
- **P3 補測多角色同框**。能力表那個 0 分標的是「推論」不是實測，值得花 2 支算力換成真數據。
- **P4 受控大動作與人手 —— 別碰**。12 支單變數實驗證明是模型層天花板。

## 兩個要更正的既有認知

1. **VRAM 不是 4GB**。Windows `Win32_VideoController` 回報 4GB 是 32 位欄位溢位的假值，
   `nvidia-smi` 實測 **8151 MiB**，compute cap 12.0。往後查 VRAM 一律用 nvidia-smi。
2. **AV1 來源的 ffmpeg 場景偵測不可信**。直接對 AV1 檔測，四種門檻（0.10–0.30）
   通通只回第 0 幀，看起來像一鏡到底；**先轉 h264 再測**才抓得到真切點。

## 🔴 未完成：爬網研究工作流

22:28 啟動了一個 11 代理的研究工作流，查「這類 AI 寵物短片實際怎麼做」，
七條線平行掃網（頻道本身／社群作法／商用工具鏈／本機模型現況／長鏡頭技術／
角色一致性／畫質補強）＋ 兩個對抗驗證（來源真偽、8GB 可行性）＋ 完整性批評 ＋ 彙整。

**收工時仍在第一階段（掃網）**，未完成。

- Run ID：`wf_a8ef5ef0-b79`（**resume 只能同 session，新視窗接不了**）
- 腳本存檔（新視窗可直接重跑）：
  `C:\Users\TUF Gaming\.claude\projects\C--Users-TUF-Gaming\19442ae7-4a86-4c75-8819-8e00b7753177\workflows\scripts\ai-pet-shorts-research-wf_a8ef5ef0-b79.js`
- 已完成代理的原始輸出：同 session 目錄下的 `subagents\workflows\wf_a8ef5ef0-b79\`
  （七個 `agent-*.jsonl`，掃網階段的原始搜尋結果都在裡面）

**新視窗要繼續的話**：用 `Workflow({scriptPath: "<上面那個路徑>"})` 從頭重跑，
約 25 分鐘。或先去讀那七個 agent jsonl 看掃網階段撈到什麼，省掉重跑。

## 這輪沒有要 commit 的產線改動

`auto/pipeline.log` 有 2 行 diff，是 20 分鐘排程器自己寫的
（`⏸ D10S1 還在等賢賢過目`），不是這輪動的。

## D12S1 那四件待裁決的事仍然沒動

見上半段「等裁決」四項。稽核報告獨立確認了其中兩項：
第 4 項項圈滑位（約 7.5–9.0 s，逐幀目視確認）與第 3 項腳掌太乾淨（物理矛盾）。

---
---

# 23:15 這輪 — 爬網研究（11 代理工作流）

**報告**：https://claude.ai/code/artifact/e30c7015-52f0-4a23-bf99-19749bcc27c1

7 條線平行掃網 ＋ 2 個對抗驗證 ＋ 1 個完整性批評 ＋ 1 個彙整。
39.5 分鐘、496 次工具呼叫、114 萬 subagent token。
**177 項發現，33 項被對抗驗證推翻，另外 2 項由本機檔案比對推翻。**

## ⛔ 最重要：不要照做的兩件事

彙整代理產出的原始報告（存在暫存區 `report.md`）有兩個硬錯，發布的頁面已經修掉，
但如果之後有人翻到原始版，要知道這兩條是錯的：

### 1. 「把 steps 從 8 降到 4，算圖時間砍半」 ← 錯

它的依據只是模型卡一句「4 steps is enough」。
但 `_exp_20260814/` 裡 12 支單變數實驗（E00–E11 ＋ 2 支雲端對照，13 份 log 都在）
證明 **steps 8 對 steps 4 全面勝出，沒有任何一項退步**：
動作 +38%、抖動 −7%、光流解釋率 +38%、角色細節 +9%。
成本也講反了 —— 4→8 是多花 30%（5.0→6.5 分鐘），不是一倍。

**維持 steps 8。** 通用模型卡 vs 自己機器上的 A/B，後者贏。

### 2. 「改 gen_10s.py 的節點 12」 ← 檔名是掰的

不存在這個檔。只有 `auto/make_video_local_5s.py` 和 `auto/make_video_cloud.py`。
（節點 `"12"` 是 `LoadImage` 這件事它說對了。）

> 教訓：爬網代理讀不到本機檔案，所以「網路通論」跟「賢賢的實測」相撞時，
> 一律採信實測。以後派研究時可以把關鍵實測結論先餵給它們。

## 三個最有價值的發現

1. **Tim and Jeffy 自己承認是 AI 做的** —— 影片 `ECx1qo3GwkE` 說明欄逐字寫
   「part of the Tim & Jeffy **AI** dog comedy series」。頻道簡介卻只掛
   「✅ 100% Original Content」完全不提 AI。
   規模：51 支、17.5 億觀看、66.7 萬訂閱、開台 56 天、一天一支、**沒有一支超過 16 秒**。

2. **全片零對白，而且有對照組驗過** —— 三支的 subtitles 與 automatic_captions 全空；
   抓 MrBeast 與 Veritasium 各 3 支會講話的 Shorts 當對照，6/6 都有自動字幕。
   → **配音與口型這兩關根本不用過。** 記憶裡「沒有堪用台語 TTS」「配音要 ElevenLabs」
   那些卡點，在這個題材裡不存在。

3. **P0 的目標可能是幻覺**（標明是推的）—— 12–16 秒 ＝ 2 段 8 秒或 3 段 5 秒硬接。
   依據：長度死鎖該區間、蛋糕切口跳變是跨段特徵、Veo 3.1 單次上限 8 秒 / Kling 2.6 是 10 秒。
   **沒有任何 2026 年的模型能單次生 9.57 秒**，所以稽核量到的那顆主鏡八成本身就是藏切接的。
   → P0 要追的不是「拉長單段」，是「把接縫藏好」，難度差一個量級。

## 重排後的三件事（已把錯的第一項拿掉）

**01 多幀接龍**（零下載，1 天）
改 `make_video_local_5s.py` 節點 `"12"`，從 `LoadImage` 單張換成餵前段末 17 格：
`LoadVideo → GetVideoComponents → ImageFromBatch(-17, 17)` 接 `Wan22ImageToVideoLatent.start_image`。
有讀 ComfyUI 0.30.0 `comfy_extras/nodes_wan.py` 原始碼確認機制：
`start_image[:length]` → encode → `mask[:,:,:latent_temp.shape[-3]] *= 0.0`
—— **餵幾格就鎖幾格 latent，KSampler 完全不動**。格數取 `4n+1`：13/17/21/25。
第二段生完用 `ImageFromBatch(batch_index=17, ...)` 切掉重複頭，
兩段之間用十行 numpy 做 Reinhard 色彩轉移對齊（不用裝 KJNodes）。
> **單張圖 ＝ 零運動資訊、零身分錨，這就是長人手／增生第二隻狗的根因。**
> 風險：5B TI2V 訓練時是單幀條件，多幀等於當 latent inpainting 用，可能不吃這套。

**02 Fun-5B-InP 做 FLF2V 雙端錨**（3.12 GB，半天，**要賢賢點頭才下載**）
`QuantStack/Wan2.2-Fun-5B-InP-GGUF` Q4_K_S ＋ `stduhpf/ComfyUI--Wan22FirstLastFrameToVideoLatent`
（5B 用新 VAE，原生 FLF2V 節點吃不了）。
**一顆同時打 P0 接得穩 ＋ P2 道具不消失 ＋ P3 雙狗位置鎖住。**
風險：沒有任何人貼過 8GB 跑 5B InP 的秒數或 VRAM 峰值，節點相容性也沒人確認過。

**03 三個零成本當天調整**
- 段間統一調色：`ComfyUI-WanVideoWrapper` 色偏 bug（issue #1541/#1277）亮度飽和度單調上升，
  被關成 not planned。**成片前對每段做一次 ffmpeg 統一調色，從選配改成必做。**
- I2V prompt 瘦身單變數測：Wan 2.2 官方公式說 **圖生視頻 ＝ 只有運動＋運鏡**，
  主體/場景/風格已由起始圖決定。`d12s1_video.txt` 那 1785 字元大量在重描述外觀，
  依公式等於在鼓勵模型重畫牠。拿同一張起始圖做 A/B。
- 量化 Q4_K_M → Q5_K_S（只多 0.26 GB，官方範例就是拿 Q5_K_S 跑 121 幀）。

## 死路（對抗驗證殺掉的重點）

- **Wan 2.5 / 2.6 / 2.7 開源 ＝ 假的**。連續三代十三個月沒放權重，
  **Wan 2.2（2025-07）仍是最後一個有開放權重的版本**。
  宣稱開源的 `wan27.org` 頁尾自承與 Alibaba 無關、掛名作者「MkSaaS」是網站模板產品名
  —— **該站整站當 SEO 內容農場看待**，它給的「提示詞超過 80 字會被忽略」「LoRA 參數表」都不可信。
- **Qwen-Image-Edit 2511 / SeedVR2 / LTX-2 / MiniMax H3 全部因 RAM 出局**，不是 VRAM。
  例：Qwen 的文字編碼器單檔 9.38 GB 必須待在系統 RAM；SeedVR2 在 12GB 卡上系統 RAM 峰值 28.5 GB。
- **授權地雷**：`4x-UltraSharp` 是 CC-BY-NC-SA-4.0 **禁商用**。
  可商用替代：`RealESRGAN_x4plus`(BSD-3)、`4xNomos8kSC`(CC-BY-4.0)、`4xPurePhoto-RealPLSKR`(CC-BY-SA-4.0)。
- **P4 大動作本機無解**，不是缺工具是缺顯卡：Wan-Animate-2 官方需求 8×A800(720P)/2×A800(480P)；
  AnimalPose ControlNet 是斷路（配對的是 SD1.5 專用權重，掛不到 FLUX 或 Wan）。
- **靠寫更好的 prompt 解決角色漂移 ＝ 做不到**。影片模型兩次生成之間沒有記憶，
  每一鏡都是獨立詮釋。要靠條件輸入（參考圖、鎖住的 latent 格），不是文字。

## 兩個 ComfyUI 設定層的提醒

- ComfyUI 約 2026-03 起預設開 Dynamic VRAM，**開著時 `--lowvram` 無效**。
  該調的是 `--reserve-vram`（留 2–4GB 給 OS）、`--vram-headroom`、`--cache-none`、`--async-offload`。
- 5050 是 Blackwell sm_120，可裝 SageAttention 2.2 預編譯 wheel 拿約 30–35% 速度，
  但**千萬不要用 `--use-sage-attention` 旗標**（走 Triton，會讓 Wan 出黑畫面），
  要改用 KJNodes 的 patch 節點選 `sageattn_qk_int8_pv_fp16_cuda`。

## 兩件正面情報

- **「動物專用的完整本機 workflow」全世界不存在。** Civitai 的 wan2.2 tag 底下 363 個資源，
  沒有一個是動物角色向，全是人物臉/服裝/髮型。這是護城河不是壞消息。
- **平台風險有牙齒**：YouTube 2026 年初終止 16 個頻道（合計 3500 萬訂閱），
  其中 Super Cat League 是 421 萬訂閱的 AI 動物頻道。觸發條件是
  「全自動零人工編輯、只換標題/角色名的模板複製、每日零變化量產」，
  **執法是頻道級的，看最近約 30 支的模式**。
  官方原則：「AI 當工具歡迎，AI 當整個創作者不行」。
  → 現行 PUBLISH_GATE「必帶 `--by-xianxian`」方向正確，繼續守著。

## 研究的已知盲區

- **Reddit 全站在這個環境打不開**（三條線都列為最大缺口）—— 而那正是 8GB 實測最集中的地方。
  **下次派研究時在 prompt 裡寫「Reddit 那塊改用 last30days 技能查」**，該技能有獨立 Reddit 來源不走 WebFetch。
- HuggingFace Spaces 與 Discord 零覆蓋（沒被擋，是沒人想到要查）。
- 中文社群（LiblibAI、B 站）只證明存在，JS 渲染＋需登入，抓不到 workflow json。
- **RTX 5050 Laptop 全網沒有對照組**，所有 benchmark 都是 3050/3060/5060/4090/5090
  —— 所以給不出任何「換 X 之後會快多少」的可信換算。

## 等賢賢裁示（累積到現在共四件）

1. **D12S1 藍腳印發不發** —— 四個問題見上半段「等裁決」
2. **D13 / D15 要不要按守門程式建議改劇本** —— 兩支都判 BLOCKED
   （D13 命中「物件掉落」1 分、D15 命中「拿取物品」1 分）
3. **要不要下載 Fun-5B-InP**（3.12 GB）
4. **要不要加記憶體到 32GB** —— 五個獨立來源都把 32GB 當實用底線。
   注意：原研究說「都要求 32GB」是誇大一級，實際寫的是「最低 16GB、建議 32GB」，
   不要拿這條說服自己非買不可。


## 2026-08-28 晚：d6s2 收工＋賢賢的下支片指示

- d6s2 v3.6 賢賢看過評「太棒了」（審片頁 artifact 3b421d5f）；**尚未說「發」，發布仍待明確一句話**。
- **賢賢指示：下次要有狗吠聲。** 配套：規則 11 要求叫聲有畫面根據，所以下支片
  **劇本要寫進張嘴吠的動作**（建議：Nova 對 Taco 吠一聲當 punchline），吠聲素材先用
  pick.py 從 sonniss 庫挑、防撞規則照舊。
- 本支製程全紀錄在 state.json d6s2 條目；H3 短段接龍法、眉毛增生時間表在 memory
  minimax-h3-local-pipeline；混音器「>11 秒事件音被吞」bug 未修，繞法見 mix_d6s2.py 註解。
