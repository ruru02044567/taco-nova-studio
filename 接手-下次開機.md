# 下次開機接手

> 更新：2026-08-15 凌晨（前一版備份在 `03-歷史交接/交接-2026-08-11-深夜.md`）
>
> **⚠️ 模型選擇已經有正式依據了，開拍前先讀
> `LOCAL-AI-STUDIO\PRODUCTION\MODEL_CAPABILITY.md`，並跑 `auto\plan_model.py`。**

## 一句話現況

**8/15 上午進入「第三階段：劇本先分流」。分流機制已驗證可用，
卡在同一個地方 —— 奸詐微笑本機從沒測過，導致所有表情類劇本都被判 VEO。
下一輪的第一件事就是把這格 benchmark 補起來。**

---

## 🔴 下一輪從這裡接（8/15 上午做到一半）

### 進度：三個故事候選都判 VEO，根因是同一格空白

用 `plan_model.py` 跑過三個低成本候選，全部 `MODEL_SELECTION = VEO`，
理由都一樣：**「微笑、奸詐微笑」evidence = untested，能力表是空的**。

| 候選 | 劇本 | M 級 | 判定 |
|---|---|---|---|
| A | Taco 前爪踩在咬壞的黑色遙控器上，抬頭奸笑 | M3 | VEO |
| B | Taco 坐在撕爛的牛皮紙箱碎片中間，露出得意奸笑 | **M2**（最低） | VEO |
| C | Taco 站在被啃咬的書旁，眼神瞄向書再看回鏡頭奸笑 | M5 | VEO |

（三個都刻意避開白對白 —— 白狗身上看不出白色物質，這是 D4 麵粉片的教訓。
C 之所以跳到 M5 是因為「再」這個字命中了「連續劇情」關鍵字，不是真的複雜。）

### 決定：先花本機一支的時間補 benchmark，不要直接燒 Veo 額度

理由：**奸詐微笑是頻道人設核心，未來每支片都會用到。**
成功的話未來所有表情戲免費；失敗的話也拿到明確結論，
知道 3 支/日的 Veo 額度該優先花在表情鏡頭上。這比今天省 15 點重要。

### 已經備好的東西

`auto\clips\exp_bench_scene.txt` —— benchmark 用的中性表情場景圖 prompt（紙箱碎片場景）。

⚠️ **第一次生的 `exp_bench_scene.jpg` 不合格，不要用**：
耳朵是垂的不是大立耳、看不到黑點眉、臉型偏圓不像吉娃娃。
根因是我把「表情完全中性」寫得太重，稀釋了品種特徵。
**prompt 已經改好了**（品種與大立耳提前並強化成 `TWO ENORMOUS ERECT TRIANGULAR EARS`），
但還沒重生。下一輪直接重生就好。

### benchmark 怎麼跑

同一張場景圖，**只改表情描述**，本機 steps 8 跑 3 支：

1. 中性 → **奸詐微笑**（smirk，嘴角單邊上揚、眼神有目的性）
2. 中性 → **得意笑**（smug）
3. 中性 → **裝無辜**（innocent）← 對照組，已知本機做得到，
   用來分辨「是表情做不出來」還是「這張場景圖有問題」

跑完更新 `auto\model_policy.json` 裡「微笑」「奸詐微笑」兩格的 `local` 分數與 `evidence`，
`plan_model.py` 會自動跟著改變行為。同時更新 `MODEL_CAPABILITY.md` 的能力矩陣。

⚠️ **過去所有本機 prompt 都寫死 `mouth stays closed`**（為了防臉部崩壞），
等於刻意繞開了表情。benchmark 一定要把那句拿掉，否則測不到東西。

### 還沒建立的兩樣（第三階段要求的）

- `PRODUCTION\TACO_EXPRESSION_BIBLE.md` —— 表情聖經，每個表情記錄眼睛／眉眼／嘴角／
  耳朵／頭部角度／身體姿勢／適合劇情／對應素材 ID
- **素材庫目錄** —— 目前專案裡沒有任何表情素材庫（`channel-art\assets` 是頻道美術，不是這個）

benchmark 產出的素材就是 Expression Bible 的第一批內容，兩件事一起做最省。

---

## 8/14 的現況（已完成，留著當紀錄）

**發了兩支（D6 盆栽、D7 傳送門），頻道累計 9 支。
賢賢看完的評語是「不會動、不流暢」—— 這是本機模型的天花板，不是 prompt 沒寫好。**

---

## ✅ 上一輪（8/14 夜～8/15 凌晨）已完成 —— 結論在 MODEL_CAPABILITY.md

12 支單變數實驗 ＋ 1 支雲端對照跑完了。三句話總結：

1. **shift 無效、sampler 維持 euler、幀數改短只是片短不是變順。**
2. **steps 是唯一有效的變數**，正式值改成 **8**（對 steps 4 全面勝出，只多 30% 時間）。
   但動作幅度買得到、**流暢度買不到** —— 速度抖動在 steps 8 之後卡死在 0.47，Veo 是 0.191。
3. **最關鍵：本機不聽動作指令。** 六種步數十二支片，prompt 寫死的「全身甩身」一次都沒生出來；
   steps 24 的動作數字漂亮，但密集幀一看，動的是「鏡頭自己推近＋狗自己走動」。
   同一張圖同一段話丟雲端，第一支就甩了。

### 🔴 下一輪要做的事

#### ① 補三格能力表（優先序見 MODEL_CAPABILITY.md 第十一節）

1. **奸詐微笑／微笑** —— 頻道人設核心，本機**從沒測過**。
   過去 prompt 一律寫 `mouth stays closed`（防臉崩），等於刻意繞開了表情。
   做法：同一張場景圖，只改表情描述，本機 steps 8 跑 3 支。
2. **雲端的角色鎖定** —— 雲端動作滿分但 8/15 實測**換臉、黑點眉消失**。
   那版我刻意沒放鎖定句，不知道是模型不行還是 prompt 沒寫。加鎖定句再跑一支就知道。
3. **非 Turbo 版 Wan**（要賢賢點頭，下載 3.43 GB）—— 唯一可能翻案本機動作能力的路。

#### ② 把雲端產線接到 Google Flow

`auto\make_video_cloud.py` **已經走不通** —— 它是為 Gemini 網頁版的「影片」入口寫的，
而那個入口已從選單消失（8/15 實測）。新入口 `labs.google/fx/tools/flow`：
免費每天 50 點、一支 15 點（約 3 支/日）、80 秒生一支、Veo 3.1 免費可用。
「上傳圖 → 送 prompt → 批准 → 下載 mp4」已經全部打通，但還沒整理成正式腳本。

⚠️ Flow 自動化的三個坑（都踩過了）：
- **截圖像素座標 ≠ 瀏覽器 CSS 座標**（差 1.29 倍）。點按鈕一律用 DOM 查
  `getBoundingClientRect`，拿截圖上量的座標去點會空點 —— 我為此浪費 8 分鐘。
- **Enter 不送出**，要點輸入框右邊那顆箭頭。
- **「批准」不是 `<button>`**，要用「最內層節點 innerText === '批准'」去找。
  而且聊天記錄裡的舊批准鈕會一直留著，找到不等於是新的那顆。

---

## 以下是 8/14 中午那一版的內容（已完成，留著當紀錄）

### 這一輪不准發布任何影片。目標是找出「本機生成能不能做出會動、流暢的片」。

先讀 `LOCAL-AI-STUDIO\PRODUCTION\D6S1_迭代紀錄與PROMPT模板.md`
和 `auto\state.json` 的 `gate_candidates`，掌握已知規律，不要重複踩過的坑。

然後做**單變數實驗**，每次只改一個，其他全部固定（同一張場景圖、同一份 prompt、同一顆 seed）：

1. **steps**：4 / 6 / 8 / 10 各生一支
2. **shift**（ModelSamplingSD3，目前寫死 8.0）：試 5.0 和 12.0
3. **幀數**：121 vs 81，看短一點會不會比較穩
4. **換 sampler**：euler 以外的
5. **查有沒有非 Turbo 版的 Wan 2.2 可裝** —— Turbo 是蒸餾版，動作弱可能就是它造成的

每支都要跑 `auto\momentum.py` 量動量，並抽 0.2 秒間隔密集幀確認「動作有沒有真的發生」，
**不要只看動量數字**（今天的教訓見下面第三節）。

最後回報一張表：哪個變數對動作幅度與流暢度有影響、影響多少，本機到底能不能達到可發布水準。
**不要給成品，給結論。**

### 同時查：Veo 免費額度恢復了沒有

頻道唯一一支 5.1 萬觀看的爆款（D1 黑洞 `y5r4mi_rjLo`）是 Veo 生的，
也是唯一一支動作真的流暢的。本機這條路今天已證明「畫面能過關、動作過不了關」。
**如果額度回來了，動作片就該走 Veo，本機留給靜態的裝無辜片。**

---

## 今天（8/14）做了什麼

### 已發布兩支

| | 網址 | 內容 |
|---|---|---|
| D6S1 | https://youtube.com/shorts/uzL5_WgI8K8 | 盆栽土，站定裝無辜，5.83 秒 |
| D7S1 | https://youtube.com/shorts/kCEOCyZRGI8 | 紫色傳送門吞拖鞋，迴力鏢剪輯，9.375 秒 |

⚠️ **一天發兩支，打亂了「一天一支」的排程節奏**（D7 原訂 8/15 08:00）。
`schedule.json` 的 `day1_date` 沒有動，要不要順移由賢賢決定。

### 本機模型天花板：今天六支候選片的實測結論

**這是這次最重要的產出，比那兩支片重要。**

| 現象 | 證據 |
|---|---|
| **生不出角色動作** | D6 v09／v10 兩顆 seed 都只有「站定轉頭」，prompt 明寫甩身也不做 |
| **鎖定句是取捨不是技巧** | 鎖定句加多 → 完全不動（D7 v02）；放鬆 → 角色崩壞（D7 v01） |
| **物件動作也做不好** | D7 v01 想演拖鞋下沉，實際演成**反向浮起**（曝露面積 +23%）＋2.7 秒後融解成灰塊 |
| **不流暢的根因** | Wan 2.2 Turbo 只跑 4 步、121 幀，時間連續性本來就撐不住（8/13 已記錄） |

⚠️ **「調 steps／shift 能不能解決」還沒有乾淨驗證過。**
8/14 試過 steps 6，但那次同時改了三個變數（場景圖＋prompt＋steps），結論不算數。
所以上面那張實驗表是必要的，不是保險起見。

### 三個新教訓（都已寫進對應文件）

**① 場景圖的角色辨識物一定要裁頭部放大逐顆數過再用。**
D7 v02 的「黑點三顆」根因是**場景圖 `d7s1_scene_b1.jpg` 本身就畫成三顆**，
重生影片幾百次都修不好。掃視全圖看不出來，我自己也看漏了。
該圖已列入 `rejected.json`。

**② prompt 裡反覆強調一個視覺元素，模型會多畫幾個。**
場景圖 prompt 寫一長串形容黑點的句子 → 生出三顆。
改成極簡＋**正面描述**「兩顆之間是乾淨白毛」就對了。
同理：否定句擋不住走位（`never walks toward the camera` 完全無效）。

**③ 迴力鏢剪輯可以解掉 Shorts 的 loop 硬跳。**
D7 末幀對首幀差 33.97（相鄰幀基準 3.50，等於 9.7 倍硬跳），
改成「正播到 4.5 秒 → 反播回首幀」之後降到 **0.66（0.2 倍）**，循環完全隱形。
適用條件：全片沒有方向性動作。做法在 `state.json` 的 d7s1.source。

### 新增的東西

| 檔案 | 做什麼 |
|---|---|
| `sfx/mix.py` 的 `soil_still` | D6 用。濕土靜態版，「安靜的犯罪現場」 |
| `sfx/mix.py` 的 `soil_shake` | 濕土甩身版，**目前沒用到**（甩身沒生出來），留著備用 |
| `sfx/mix.py` 的 `portal` | 傳送門 5 秒版，改編自 blackhole，**目前沒用到** |
| `sfx/mix.py` 的 `portal_boomerang` | D7 用。聲音弧線跟著鏡頭距離走：推近漸強、中點最強、拉遠漸弱 |
| `auto/make_video_local_5s.py` | 加了 `--steps` 參數（預設 4，向後相容） |
| `auto/clips/d7s1_scene_c.txt` | D7 場景圖 prompt，黑點寫法是目前最穩的版本，可沿用 |

---

## ⚠️ 環境重點（會踩的坑）

| 項目 | 正確作法 |
|---|---|
| **跑產線腳本的 Python** | **系統 Python**：`C:\Users\TUF Gaming\AppData\Local\Programs\Python\Python313\python.exe`（playwright 在這裡）。**不是** ComfyUI 的 venv |
| **跑訓練/生圖** | ComfyUI venv：`C:\Users\TUF Gaming\ai-video-local\venv\Scripts\python.exe` |
| **發布前的頻道檢查** | **第一次一定會失敗**（Studio 冷啟動還沒轉址到 `/channel/<id>`）。查 `http://127.0.0.1:9222/json` 確認 URL 是 `UC4Bf0lB05GrYF8Q4l6NnjEA` 就直接重跑，會過。今天兩支都是這樣 |
| **改 JSON** | 用 Python 改，**不要用 Edit 手改**。今天手改 `rejected.json` 漏一個逗號，而 `publish_video.py` 讀不到黑名單時會**靜默跳過檢查**，等於保護失效 |
| **生成前** | 先 `POST http://127.0.0.1:8188/free`（`{"unload_models":true,"free_memory":true}`），VRAM 只有 8.5G |
| **中文路徑** | `cv2.imread` 讀不了，改用 PIL |

---

## 📁 重要文件位置

```
LOCAL-AI-STUDIO\
├── PRODUCTION\PUBLISH_GATE.md              發布門檻十三條（含規則 10 非預期讀法）
├── PRODUCTION\BEST_KNOWN_WORKFLOW.md       目前唯一能過 gate 的流程
├── PRODUCTION\D6S1_迭代紀錄與PROMPT模板.md  ⭐ 可重用的 prompt 骨架與鎖定句對照表
├── PRODUCTION\_gate_d6s1\                  D6 各版審片幀
└── PRODUCTION\_gate_d7s1\                  D7 各版審片幀（v01/v02/v03/final）

auto\state.json          每支片的完整來歷與 gate_candidates（含失敗版的量測數據）
auto\rejected.json       黑名單 9 筆，publish_video.py 會擋
```

---

## 這份文件裡哪些話該打折

- **兩支新片都還沒有任何流量數據**，今天才發。上面寫的都是技術指標不是成效。
- **「本機做不出動作」是六支候選片的歸納**，不是模型規格文件寫的。
  換 steps／shift／非 Turbo 版之後會不會翻案，就是下一輪要驗的事。
- **多代理審片今天抓到兩次主模型看漏的硬傷**（v01 融解、v02 三顆黑點），
  值得繼續用；但它也不是萬能，賢賢看完兩支成品的評語「不會動、不流暢」
  是四個 lens 都判 PASS 之後才講的 —— **審片能擋硬傷，擋不掉「無聊」。**
