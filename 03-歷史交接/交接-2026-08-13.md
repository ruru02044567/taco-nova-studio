# 交接 — 2026-08-13

> 給下一個對話視窗。上一個視窗跑了整晚，內容太長所以換窗。

---

## 一句話現況

**D4S1 麵粉片今天 10:48 已發布（第 7 支），Nova V2 LoRA 訓完但只解決一半問題，
而且查出真正的瓶頸是 IP-Adapter 不是 LoRA。**

---

## ✅ 今天完成的

### 1. 發布了 D4S1（頻道第 7 支）

```
https://youtube.com/shorts/PLzHqmJDGYo
檔案  待審核\d4s1-甩身-有聲.mp4（1080×1920 / 5.04s / aac / seed 424243）
```

這支的來歷：8/11 原版被賢賢一眼看出「像在吐白粉」→ 退回 → 8/13 凌晨改成「原地甩身」
→ 夜間三顆 seed 挑出 424243（77 分）→ 配 `flour_shake` 音效 ＋ 改文案 → 發布。

**規則 10（非預期讀法）就是因為這支寫進 `PUBLISH_GATE.md` 的。**

### 2. Nova LoRA V2

```
NIGHT_RD\out_nova_v2\nova_lora_V2_step{250…1500}.safetensors
訓練集 12 張（乾淨5 / 淹水3 / 麵粉2 / 岩漿2）｜LR 5e-5｜1500 步｜26.7 分
```

**證實**：加入髒亂場景後，權衡曲線右移一格 —— V1 在 LoRA 0.25 就把紅酒漬抹掉，V2 撐到 0.35。

**推翻**：以為「Nova 身份問題是 LoRA 不夠好」。實測拿掉 IP-Adapter 後眼睛才變藍
→ **IP-Adapter 蓋掉了 LoRA**，這是下一步最該處理的。

### 3. 髒污顏色對照實驗

白／淺灰／淺棕**都畫不出附著在白毛上**，只有**黑色**明顯附著。
→ 不要再攻白粉題材。排程上的 **D11S1 黑漆片**是驗證「甩落」效果最直接的題材。

---

## 🔴 下一步（優先序）

1. **測「LoRA 取代 IP-Adapter」** —— 腳本已寫好但**還沒跑**：
   `NIGHT_RD\26_ab_ipadapter.py`
   A：ControlNet ＋ Nova V2 LoRA 0.45，無 IP-Adapter
   B：ControlNet ＋ LoRA 0.35 ＋ IP-Adapter 0.65
   兩組共用同一份 prompt，seed 424242。**不用再訓練，這是最有機會突破的方向。**
2. 生 **D6S1 盆栽片**（排程下一支）
3. 用 D11S1 黑漆片驗證甩落效果

**不要做**：再訓 V3、再抽 seed、再攻白粉。

---

## ⚠️ 環境重點（會踩的坑）

| 項目 | 正確作法 |
|---|---|
| **跑產線腳本的 Python** | **系統 Python**：`C:\Users\TUF Gaming\AppData\Local\Programs\Python\Python313\python.exe`（playwright 在這裡）。**不是** ComfyUI 的 venv |
| **跑訓練/生圖** | ComfyUI venv：`C:\Users\TUF Gaming\ai-video-local\venv\Scripts\python.exe` ＋ `PYTHONPATH=LOCAL-AI-STUDIO\_trainlib` |
| **發布用的瀏覽器** | 獨立 profile `C:\Users\TUF Gaming\.config\gemini-bot-profile`，**不是賢賢日常用的 Chrome/Edge**。視窗被設在 `2000,2000`（螢幕外），要看得到得自己改位置 |
| **發布前** | 那個 bot Edge 要先暖機（剛啟動時 Studio 不會轉址到 `/channel/<id>`，檢查會誤擋，等 30 秒再跑就過） |
| **CLIP token 上限 77** | 生圖 prompt 超過就被硬切。已犯兩次，寫 prompt 前先數 |
| **RAM 是真瓶頸** | 訓練前先 `comfy_api.free_memory()`，確認可用 ≥ 4 GB。VRAM 紅線 7 GB |
| **中文路徑** | `cv2.imread` 讀不了，改用 PIL；`ls -l` 欄位會被家目錄空格擠掉，用 `stat -c%s` |
| **開檔案** | `explorer.exe` 對中文檔名不穩，用 `Invoke-Item -LiteralPath` |

---

## 📁 重要文件位置

```
LOCAL-AI-STUDIO\
├── PRODUCTION\PUBLISH_GATE.md          發布門檻十三條（含規則 10）
├── PRODUCTION\BEST_KNOWN_WORKFLOW.md   目前唯一能過 gate 的流程
├── PRODUCTION\PRODUCTION_QUEUE.md      發片佇列
├── NIGHT_RD\REPORT_2026-08-13.md       昨夜完整報告（PARTIAL）
├── NIGHT_RD\26_ab_ipadapter.py         ★ 下一步要跑的腳本
└── DATASET\dog_support_v2\             Nova V2 訓練集 ＋ manifest

桌面\財富密碼-完整報告.md                17 份文件整併版（25 KB）
```

---

## 未解決／待決定

- `sync_gbrain.py` 每次發布會印 `SKIP`（G-Brain 已停用）。**無害**，包在 try 裡不影響發布
- `pipeline.py` 的 `cmd_review` 顯示的檔名是自己拼的，跟實際發布的檔不同（顯示 bug，不影響功能）
- bot Edge 視窗位置要不要改成看得見（改 `pipeline.py` 一個座標）
- 排程 3 個都還停用中（賽道週報、TacoNova-Pipeline、DailyReport）

**排程共 19 支，已發 7 支，還有 12 支未發。**
