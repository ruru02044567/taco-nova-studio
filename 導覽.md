# 財富密碼 — 總入口

> Taco & Nova（機歪吉娃娃 × 哈士奇）YouTube Shorts 頻道。
> 迷路的時候看這份，其他文件都從這裡連出去。

## 🔴 現在該做什麼

看 **[接手-下次開機.md](接手-下次開機.md)** —— 那份是活的，每次收工前更新，裡面有：
最優先待辦、今天踩到的坑、下一支拍片前務必套用的規則。

其他文件都是「查的」，只有這份是「做的」。

---

## 資料夾地圖

### 活的（每天會動）

| 位置 | 放什麼 |
|---|---|
| `auto/` | **產線本體**。`pipeline.py` 是主入口，`schedule.json` 排程，`state.json` 進度 |
| `待審核/` | 產線輸出的成片，等賢賢過目。**發布前一定先看這裡** |
| `每日體檢/` | `daily_report.py` 自動產出的頻道數據 |
| `接手-下次開機.md` | 交接文件，收工前更新 |
| `增長實驗紀錄.md` | 實驗與結果 |

### 資產（不可重現，弄丟就沒了）

| 位置 | 放什麼 |
|---|---|
| `character/` | **定裝照**。生正片餵給 Veo 的參考圖，重跑不會得到同一隻 Taco |
| `sfx/lib/` | 音效庫（Sonniss，可商用免標註） |
| `channel-art/` | 頻道美術工作檔 |
| `channel-avatar.png`、`channel-banner.png` | 已上線的頻道視覺 |

### 查的（不常動）

| 位置 | 放什麼 |
|---|---|
| `01-企劃/` | IP 總覽、對標計畫、主題提案、開拍腳本、發布資訊 |
| `02-技術選型/` | 六份選型報告 ＋ 本機生圖實測報告 |
| `03-歷史交接/` | 過期的交接文件，留著查前因後果 |
| `爆款研究/` | 對標頻道的爬取資料 |

### 實驗紀錄（做完就不動了）

`ab-test/`、`hand-test/`、`wakeup-test/`、`本機生圖測試/`、`video-01/`
—— 留著當證據，別刪，但也別在裡面繼續做新東西。

### 自動生成（可以隨時清）

`auto/_診斷截圖/`、`auto/_log/`、`auto/_舊版備份/`、`auto/__pycache__/`
—— 腳本跑一次就重生，清掉不影響任何事。

---

## ⚠️ 不能動的路徑（動了產線當場壞掉）

腳本用 `Path(__file__).parent` 往上定位，有些還寫死絕對路徑。**以下一律不准改名、不准搬家**：

| 路徑 | 誰綁著它 |
|---|---|
| 專案資料夾名「財富密碼」 | `auto/upload_branding.py:13`、`hand-test/` 三個檔（寫死絕對路徑） |
| `auto/` | 全部腳本的基準點 |
| `待審核/` | `make12.py`、`make15.py`、`finish_d9s1.py` 的輸出目標 |
| `sfx/lib/` | `make12.py`、`make15.py`、`finish_d9s1.py` 讀音效 |
| `character/` | `pipeline.py:24`、`channel-art/crop_assets.py` |
| `每日體檢/` | `daily_report.py:19` |
| `channel-art/preview/` | `sfx/wave_compare.py:35` |
| 根目錄 `taco-*.mp4` | `channel-art/video_overview.py:20-25` |
| 根目錄 `channel-avatar.png`、`channel-banner.png` | `auto/upload_branding.py:14` |
| `auto/pipeline.log`、`auto/*.status.txt` | 活的狀態檔，腳本邊跑邊寫 |

**要搬這些之前，先 grep 一次確認沒人引用。**

---

## 新東西放哪（照這個放就不會再亂）

- 寫給人看的文件 → `01-企劃/`（企劃類）或 `02-技術選型/`（技術結論）
- 一次性的診斷腳本、測試 → 開 `auto/_試驗/`，不要丟 `auto/` 根層
- 產線正式腳本 → `auto/` 根層（這裡只放正式的）
- 成片 → 讓產線自己輸出到 `待審核/`，不要手動丟根目錄
- **換一個專案還會發生的坑** → 寫進 `C:\Users\TUF Gaming\.claude\G-Brain\02-環境\已知地雷.md`，不要留在這裡

---

*2026-08-11 建立。目錄整理過一次，沒刪任何檔案，被腳本引用的路徑全部留在原位。*
