# 交接：2026-08-10（給下一個對話視窗）

> 舊視窗跑太長要重開，這份是接手用的。**先讀這份，再讀 `auto/README.md` 和 `增長實驗紀錄.md`。**

## 一句話現況

頻道有流量（10 萬觀看）但**訂閱轉換爛**（0.091%，健康值 0.3–1%）。
今天 Veo 一直逾時，改用本機生了一支 5 秒片發布，**目前零播放**。

## 今天已完成

| 事項 | 結果 |
|---|---|
| D2S1 發布 | https://youtube.com/shorts/MIWNqt3w3qM 《Day 2: Chihuahua Sits On Melted Chocolate To Hide It 🍫😳》 |
| 生成方式 | **本機 ComfyUI 單段 5 秒**（Veo 當天逾時三次）＋ Sonniss 真實音效，音量 -10.4dB |
| 產線改良 | `pipeline.py` 加了 `LEAD_HOURS = 16`：**提前 16 小時開始生片**，發布仍照原時刻。解決「08:00 才開工必定遲到」 |
| `make_video_cloud.py` | timeout 8 分鐘 → **20 分鐘**（Veo 尖峰真的會跑超過 8 分鐘，不是失敗） |
| 新工具 | `make_video_local_5s.py`：本機單段 5 秒，不接龍 |
| 題材對調 | D2S1（紅酒）↔ D9S1（巧克力）。紅酒需要連續動作，本機做不到，留給 Veo |

## 待辦（依優先序）

### ① 查清楚：五秒片為什麼零播放 ⭐ 最重要
發布於 8/10 上午，至今 0 觀看。可能原因（都還沒驗證）：
- 片長只有 5 秒，YouTube 可能對過短 Shorts 降推薦
- 剛發布還沒進推薦池（要看發布後幾小時的曲線）
- 標題改用 `Day 2:` 開頭影響搜尋
**先看 Studio 數據再下結論，不要憑猜。**

### ② 頻道美術上架（10 分鐘，0 元，投報率最高）
另一個視窗做好了但**還躺在資料夾沒上線**：
- `channel-art/preview/banner-v2.png`（含「ROAD TO 1K SUBSCRIBERS」進度條）
- `channel-art/preview/avatar-v3.png`
兩個小問題：頭像底部文字在 48px 下會糊（建議拿掉）、去背有白邊。

### ③ 三鏡 15 秒實驗（賢賢指定，還沒開始）
本機單段上限 5 秒（接龍第二段三次實測全崩）。做法是
**3 張場景圖 → 3 支 5 秒 → 剪在一起**，約 25 分鐘。

### ④ Veo API（$300 走 Vertex AI）
**結論修正過**：$300 抵免額**不能**用於 AI Studio 的 Gemini API，
但 **Veo 在 Vertex AI 是 Google 第一方模型**，應該可用。
驗證法：註冊後生一支測試片，看帳單是從免費額度扣還是實付。一支約 NT$16。

## 進行中的實驗（不要中途改動）

**實驗 #1：標題加系列編號**，範圍 D2S1／D3S1／D4S1。
變數**只有標題**，其他一律不動。D4S1 發布滿 48 小時後才判讀。
細節見 `增長實驗紀錄.md`。

## 今天學到、別再踩的坑

1. **Veo 逾時 ≠ 失敗**：尖峰時段一支超過 8 分鐘很正常，別急著重試（重試只會再排一次隊）
2. **本機只能單段 5 秒**：接龍第二段必崩（長人手／增生第二隻狗／換品種）
3. **前情塞進場景圖**：影片模型演不出物件交接，但靜態圖毫無難度。能搬到生圖階段的就別留給影片
4. **中文 `.ps1` 要存成 UTF-8 with BOM**，否則 PowerShell 5.1 用 cp950 讀 → 語法錯誤
5. **賢賢的對話視窗跑在 VS Code 裡**（不是獨立的 Claude app），關 VS Code 會斷對話
6. **兩個視窗會給出矛盾建議**：判準是「來源有沒有回答到你這條路」，不只是有沒有來源

## 機器現況

RTX 5050 Laptop 8GB VRAM／16GB RAM（**只插一條，還有一槽空著**）／認可記憶體 23GB 已超賣。
吃最重又用不到的：VS Code 2.1GB、Chrome 1.9GB、McAfee 認可 1.77GB、NVIDIA Overlay、RadeonSoftware。
