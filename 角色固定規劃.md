# 角色固定規劃（2026-08-29 19:50 台北）

賢賢裁示：以後不只開一個頻道，**每個頻道的角色必須固定**。
他問：是不是要連續做 5 片 5 秒動作量多的影片來「訓練角色」？

---

## 一、先澄清機制：拍片不會訓練到任何東西

**H3 沒有記憶。** 每次生成都是從零開始，跑第 100 支跟跑第 1 支的模型狀態一模一樣。
連拍 5 片不會讓它「學會」角色長什麼樣。

角色固定的唯一機制是 **i2v 的首幀圖**——你餵哪張圖，它就照那張圖演。
這是已經驗過的，不是推的：

| 證據 | 來源 |
|---|---|
| H3 三段接龍 15.4 秒，臉／髮型／衣服跨兩個接點都沒漂 | 2026-08-28 早，`run_h3_girl15.py` |
| 同題用 Wan：臉從第 4 秒起漂，後半形同換人 | 2026-08-28 午，`gen_wan_girl15.py` |
| Taco 用 H3 i2v：臉不崩、黑點眉不增生（Wan 十裡挑一的關卡） | 2026-08-28 晚，d6s2 |

**所以「角色不固定」的真因不是拍不夠多，是起始圖來源不統一。**
已知案例：`d6s1_scene_v2.jpg` 原圖眉毛是 3 顆點，發布版成片是 2 顆——
兩張圖不同源，角色就從這裡開始分岔。

真正的「訓練」是 **LoRA**（要 15–30 張同角色圖）。但本機紀錄顯示
taco LoRA 掛不上 FLUX/Wan；H3 能不能掛角色 LoRA **沒驗過**。這條是備案，不是主線。

## 二、你的直覺對，只是名字要改：「角色定裝五連測」

5 片 5 秒動作片該做，但用途是兩件事，都不是訓練：

1. **壓力測試**——測角色在跑／跳／急轉頭／大角度下會不會崩，
   產出一張「安全動作清單」和「禁區清單」。
   （已知禁區：手部特寫、畫面文字、大角度擺動會閃第三顆眉點）
2. **回收定裝素材**——通過的那些幀抽出來當新的起始圖，
   累積成「同一隻狗的多角度／多表情圖庫」。下次要側面鏡頭就有側面的錨點。

跑完 5 支，你得到的不是一個被訓練過的模型，
是**一組經過驗證的起始圖 ＋ 一份知道哪裡會崩的清單**。這才是資產。

## 三、每個角色的四樣標準配備（多頻道通用）

一個角色一個資料夾，缺一不可：

1. `角色聖經.json`——品種、毛色、記號、配件、禁止事項（Taco 已經有）
2. `定裝照組`——正面／側面／俯角／表情，**全部從同一張母片衍生**，不准另外生
3. `prompt 模板`——固定前綴只換動作，前綴一個字都不改
4. `禁區清單`——已知會崩的東西，選題階段就擋掉

第 2 條是重點：**新圖一律從母片衍生，不從文字重生。** 這條沒守住，前面三條都白做。

---

## 四、對標片拆解（aAPFQanOAhg，Felinemeow Ai）

實測數字（yt-dlp + ffmpeg 量的，不是看的）：

| 項目 | 數字 |
|---|---|
| 長度 / 規格 | 15.09 秒、直式、24fps |
| 頻道訂閱 | 5.83 萬 |
| 這支觀看 | 1,162 萬（上傳隔天） |
| **鏡頭數** | **1 顆。scene 閾值降到 0.15 仍測不到任何切點——單鏡到底、機位全程不動** |
| 音量 | mean −27.5 dB、max −1.3 dB（動態極大，安靜段很多） |

**劇情弧（四拍，15 秒）**：
1. 0–4 秒｜棕色吉娃娃盯著兩個化妝海綿和兩碗顏料（建立目標）
2. 4–9 秒｜牠用海綿沾黑色顏料，**把自己整隻塗成黑色**
3. 9–13 秒｜變黑的吉娃娃裝成另一隻狗，大隻鬥牛犬叼著熱狗走過來
4. 13–15 秒｜大狗低頭審視牠，收在「懷疑但沒拆穿」

**三個可以直接抄的打法**：
- **場景是角色的一部分**——同一個客廳、木地板、灰沙發、白茶几、窗光，每支都不換。
  觀眾靠場景認頻道，這同時也大幅降低生成難度（背景不用重新想）。
- **一顆機位撐 15 秒**，零剪接。跟我們筆記裡「一顆主鏡撐全片是 d10+ 公式」完全吻合。
- ⭐**牠把「角色會變」直接寫成劇情**——主角中途從棕色變黑色，
  這不是角色一致性失守，這是梗本身。**角色漂移可以當劇情用，不一定要硬扛。**

**存疑（這是推的，還沒驗）**：mean −27.5 dB 遠低於我們自己的 −14～−16 dB 驗收線，
可能代表這支**沒有鋪滿 BGM**，只有環境音＋音效點綴。跟我們現行做法相反。
驗法：下次開機跑一次頻譜，看有沒有持續的音樂軌。

---

## 五、下次開機一鍵開跑

```bash
# 1. 起引擎（先跑，載模型要幾分鐘）
cd "/c/Users/TUF Gaming/ai-video-local"
./venv/Scripts/python.exe ComfyUI/main.py --port 8188 &

# 2. 確認活著
curl -s http://127.0.0.1:8188/system_stats

# 3. 生片（三段接龍 15 秒，首幀鎖 Taco 定裝照）
cd h3-tools
python run_h3_chain.py <輸出.mp4> --first-frame "<定裝照路徑>" 480 832
```

素材位置：
- Taco 定裝照：`財富密碼\character\taco-ref*.jpg`（v2-sly / v3a-con / v4b-glare 等 7 張）
- 角色聖經：`財富密碼\character\角色聖經.json`
- 對標片與抽幀：`C:\tmp\bench\`（**暫存區，關機可能被清，要留就搬走**）
- 曲庫：`ai-video-local\music\`（PeriTune CC-BY，對外發布要附出處）

## 六、時間估算（有實測基礎，不是拍腦袋）

單段 124 幀 ＝ 5.2 秒。實測速度：

| 解析度 | 每段 | 15 秒（三段） | 加後製驗片 |
|---|---|---|---|
| 352×608 直式 | 6.7 分 | 20 分 | **約 30 分** |
| 480×832 直式（Taco 慣用） | 8.8 分 | 26 分 | **約 35–40 分** |
| 1024×576 | 11.8 分 | 35 分 | 約 45–50 分 |

- 只要 5 秒單發：**約 12 分鐘**
- **第一支要另加 15 分前置**（挑定裝照、寫 prompt 模板、對齊對標結構）→ 首支約 50 分鐘
- 之後同角色再出片，就只剩純生成時間

⚠️ 一張顯卡＝產線互斥鎖，開跑前先確認沒有別的視窗在用 GPU。
