# 選型：產線與人力 —— 一個人日更，撐不撐得住？

> 2026-08-10 21:36（台北）｜ 匯率 1 USD ≈ 31.5 TWD
> 這份不談技術最優，只談**人受不受得了**。前三份報告（硬體、賽道標竿、畫質與演算法）談的是「該買什麼」，這份談「這條線一個人跑得動嗎」。

---

## 一句話結論

**日更「發布」現實，日更「製作」不現實。把這兩件事拆開，就解決了 80% 的問題。**

做法：一週開工一次（3–4 小時做 7–10 支），囤成 14 支的存貨，每天從存貨自動發。
你今天的痛苦不是「事情太多」，是「事情太碎、隨時被打斷、而且會失敗」——
真正的敵人是**打斷次數**，不是工時。

配套的三個技術決定：
1. 把瀏覽器遙控整套砍掉，改走 API（今天那場災難的根因就是它）
2. 審核搬到手機上、加機器預審，把 9 小時 18 分壓成 30 秒
3. 一稿多投別自己串 OAuth，租現成的（月費 NT$504）

---

## Q1. 一支片實際要花多少人力？

先講一個關鍵區別，這是整份報告的地基：

| 名詞 | 白話 | 你的實況 |
|---|---|---|
| **人力時間**（touch time） | 你的手真的在動的時間 | 一支約 **35–50 分鐘** |
| **掛鐘時間**（lead time） | 從開工到發布經過多久 | 一支 **10–20 小時** |
| **打斷次數** | 這支片逼你回頭看它幾次 | 一支約 **5–8 次** |

工廠管理有句老話：**人力時間決定成本，打斷次數決定你會不會放棄。**
一天 50 分鐘誰都撐得住，但一天被打斷 7 次、每次都要重新進入狀況，兩週就爆炸。

### 逐步拆解（依 `auto/pipeline.log` 的實際時間戳推算）

| 步驟 | 機器時間 | 人力時間 | 打斷 | 誰做 | 能不能全自動 |
|---|---|---|---|---|---|
| ① 選題、想笑點 | — | **15–25 分** | 1 | 人 | ❌ 不能（但可以「AI 出題、人只刪不想」，省一半） |
| ② 寫 prompt、場景描述 | — | 5–10 分 | — | 人＋AI | 🔶 半自動（有模板後 AI 產、人審） |
| ③ 生場景圖 | 26 秒 | 1 分 | 1 | 機器 | ✅ 可以（改 API 後） |
| ④ 生影片（Veo 網頁） | **順利 2–20 分／今天 3 小時失敗 3 次** | **順利 0 分／不順 30–90 分** | **1–5 次** | 機器（但一直在叫人） | ✅ 可以（改 API 後） |
| ⑤ 配音效（Sonniss ＋ ffmpeg） | 2 分 | 5–10 分 | 1 | 人挑、機器混 | ✅ 可以（建「音效包」對應題材類型） |
| ⑥ 寫標題／說明／hashtag | — | 5 分 | — | AI 產、人挑 | ✅ 可以 |
| ⑦ **審核** | — | **2 分（但等了 9 小時 18 分）** | 1 | 人 | 🔶 機器預審＋人終審 |
| ⑧ 上傳發布（Playwright 遙控 YouTube） | 3–5 分 | 2 分（要看著它別出事） | 1 | 機器 | ✅ 可以（改 YouTube API 後） |
| ⑨ 看數據 | — | 10 分／天 | 1 | 人 | 🔶 自動出日報，人只看結論 |

**順利的一天：約 40 分鐘、被打斷 5 次。**
**今天這種一天：約 3.5–4 小時、被打斷 8 次以上。**（光 D3S1 一支就跑了 17:21 → 20:07 三輪全敗）

### 三個真正的瓶頸

**瓶頸一：Veo 網頁遙控（機器瓶頸，但吃的是人的時間）**
它不是「慢」，是**會失敗、而且失敗得很像成功**。`pipeline.log` 裡的
「靜默還原了（框內有字=False, 影片模式=False, 附件=True）」——
頁面自己把設定吃掉了，腳本以為送出去了，其實什麼都沒發生。
這種錯誤沒辦法自動處理，每次都要人去看截圖判斷。**這一步吃掉你 60% 的痛苦。**

**瓶頸二：審核（人的瓶頸，但根源是流程設計）**
02:06 生好、11:24 才核准，卡了 9 小時 18 分。
問題不在你睡覺——問題在**你睡覺的時候，整條產線是停的**。這是排程設計錯誤，不是人的錯。

**瓶頸三：選題（唯一真正無法自動化的，但沒人在管它）**
最該花時間的一步，現在反而被前兩個瓶頸擠掉了。
你的訂閱轉換率 0.091% 是內容問題，內容問題就是選題問題。
**把時間從跟瀏覽器搏鬥搬回選題，這條線才有意義。**

### 哪幾步「一定」要人？只有三件

1. **選題與笑點**——AI 現在寫的寵物梗都很平，這是你的護城河
2. **最終品味判斷**——這支好不好笑、Taco 是不是 Taco
3. **看完數據的方向決策**——哪類題材加碼、哪類砍掉

**其他全部可以自動化。** 現在你手上 90% 的動作，都不屬於這三件。

---

## Q2. 日更現實嗎？該用什麼節奏？

### 直接回答：日更製作不現實，日更發布很現實

業界數字（來源是行銷公司的部落格，可信度中等，當方向參考不當證據）：
62–90% 的全職創作者回報 burnout，47% 在過去半年想過退出。
但你不需要看別人的數字——**你開台第 5 天，就已經有一整天耗在跟瀏覽器搏鬥了。**
這不是「兩週後可能會發生」，是已經發生了。

### 批次 vs 每日：正面比較

| | 每日生產（現在） | 批次生產（建議） |
|---|---|---|
| 開工次數 | 30 次／月 | **4–5 次／月** |
| 每次時長 | 40 分（順）～ 4 小時（不順） | 3–4 小時（可預測） |
| 月總工時 | 20–40 小時 | **14–18 小時** |
| API 掛掉的後果 | **當天斷更** | 沒事，吃存貨 |
| 你生病／出差 | **斷更** | 沒事，吃存貨 |
| 心理負擔 | 每天早上醒來先擔心片子 | 週日搞定，其餘六天不想它 |
| 內容品質 | 趕出來的 | 一次想 10 個題材，可以互相比較挑最好的 |

批次贏在一個不明顯但致命的地方：**你一次想 10 個題材時，會自然而然砍掉最爛的 3 個。
每天想 1 個時，那個爛的一定會被發出去。** 你的 D2S1（0 觀看）、D3S1 就是這樣來的。

### 建議節奏：週一批次制

```
週日下午 3–4 小時（唯一的開工日）
  14:00–14:40  選題：AI 產 30 個候選 → 你只做「刪」，留 10 個
  14:40–15:10  寫 prompt（有模板，AI 產、你改）
  15:10        按下 Enter → 走 API 批次生成，10 支同時跑
               ★ 這段你去做別的事，機器自己跑 30–60 分鐘
  16:10–16:30  審核 10 支（看四格圖，一支 2 分鐘）
  16:30–16:50  排程上傳，一次排完未來 10 天

週一至週六：0 分鐘
  每 2–3 天花 10 分鐘看一次日報（手機上看就好）
```

**存貨目標 14 支（兩週份）。警戒線 5 支——低於 5 就加開一場批次。**
為什麼 14：一次批次全滅（生病、API 大當機、Google 改版）你還有一週緩衝，不會斷更。
YouTube 排程最遠可排 6 個月，容量遠遠夠。

### 過渡期（存貨還沒建起來的兩週）

現在存貨是 0，不能一夕之間切過去。建議：
- **第一週**：先降到隔日更（週三、五、日發），把省下的時間全部拿去建存貨
- **第二週**：存貨到 7 支就恢復日更
- 心理上要接受：**斷更一週去建產線，比撐兩週後徹底放棄划算得多。**

---

## Q3. 改用 API 之後，產線該長什麼樣？

### 今天到底出了什麼事（先講清楚，才知道要修什麼）

`auto/pipeline.log` 和 `auto/README.md` 記錄了三個症狀，其實是同一個病：

| 症狀 | 實際原因 |
|---|---|
| 服務端錯誤偽裝（「發生錯誤 (1155)」、「請檢查網路連線」） | 網頁不會回錯誤碼，只會回一段給人看的字。腳本永遠在**猜**現在是什麼狀況 |
| 跟手動操作搶瀏覽器 | 排程器和你共用同一個 Edge，誰先 `goto()` 誰就把對方的對話沖掉 |
| 頁面被導走、設定被吃掉 | 網頁是給人用的，它有自己的狀態機，你只是在旁邊模擬點擊 |

**根因一句話：你在用「模擬人類點滑鼠」的方式跟一個沒有合約的介面對話。**
`browser_lock.py` 是很聰明的補丁，但它在補的是一個不該存在的問題。

### 重構後的架構

```
┌─ 內容層（人，週日 1 小時）──────────────────┐
│  jobs.db  ← 選題、prompt、標題、發布時間        │
└──────────────┬─────────────────────────────┘
               │
┌─ 生產層（機器，無人值守）────────────────────┐
│  worker.py（常駐，asyncio，同時跑 3–5 支）      │
│    ├─ gen_image.py   Gemini 3.1 Flash Image API │
│    ├─ gen_video.py   Veo 3.1 Fast API（poll）   │
│    └─ mix_audio.py   ffmpeg ＋ Sonniss 音效包    │
└──────────────┬─────────────────────────────┘
               │
┌─ 審核層（人，手機，30 秒）────────────────────┐
│  precheck.py  機器預審 → 明顯壞的直接退回重生    │
│  review_bot   四格圖推到手機 → 回「1」或「2」    │
└──────────────┬─────────────────────────────┘
               │
┌─ 發布層（機器，零打斷）──────────────────────┐
│  publish_api.py  YouTube Data API v3            │
│                  status.publishAt 直接排程       │
│  crosspost.py    Upload-Post → 其餘四平台        │
└─────────────────────────────────────────────┘
```

### 六個具體的重構動作

**① 砍掉工作排程器的 20 分鐘輪詢，改成常駐 worker**

現在的架構是「每 20 分鐘醒來一次，猜猜看該做什麼」。這是因為網頁遙控沒辦法等待——
一等就卡住整支腳本。API 不一樣，它給你一個 operation id，你可以慢慢 poll，
而且**可以同時 poll 十支**。

改成一支 `worker.py` 常駐跑 asyncio。好處：
- 十支片同時生成，總時間從「10 × 5 分鐘」變成「約 6 分鐘」
- 不再有「20 分鐘窗口內剛好沒醒來」的空轉（今天 13:05–15:45 那八行「目前沒有到期的工作」）
- 不再需要 `browser_lock.py` ——沒有瀏覽器就沒有鎖

**② 把 `schedule.json` 換成一張真正的工作表（SQLite 或 jobs/ 資料夾）**

現在 `state.json` 混著記進度和結果，`schedule.json` 有 122 KB 又是七天固定的。
改成每支片一筆記錄，狀態機明確：

```
draft → scene_ready → video_ready → precheck_pass → approved → scheduled → published
                                 ↘ precheck_fail → regenerate（自動，不吵人）
```

關鍵是**每一步都要存進度到磁碟**。今天最痛的是「重跑＝重新排隊」——
API 版一定要把 operation name 存起來，程式重啟後**接著 poll，不是重生**。
（一次重生 = 再付一次錢，這跟網頁版免費重試是完全不同的心態）

**③ 錯誤處理要分類，不能全部一視同仁**

網頁版只能看字串猜，API 會回明確的 HTTP 狀態碼：

| 錯誤類型 | 該做什麼 | 現在的做法 |
|---|---|---|
| 400 參數錯（prompt 被擋、比例不支援） | **不要重試**，直接標記待人處理 | 盲目重試 8 次 |
| 429 限流 | 指數退避重試 | 盲目重試 |
| 5xx 服務端 | 短退避重試 3 次 | 盲目重試 |
| 配額用完 | 等重置 | 這個有做對 |
| 安全過濾擋掉 | **改 prompt**，重試沒用 | 盲目重試 |

**API 版重試要花錢**，所以 `VEO_BACKOFF_H = [0.1, 0.1, 0.35, ...]` 這種「先快速重試」
的策略要反過來——先確認錯誤類型，再決定要不要重試。

**④ 生圖也一起走 API，不要留半條瀏覽器產線**

這點很重要：**只要還有一個步驟需要瀏覽器，你就還是得維護整套 Edge/CDP/lock，
所有問題都會回來。** 現在保留網頁生圖的唯一理由是「免費」，但算一下帳：

Gemini 3.1 Flash Image 1K 一張 $0.067（≈ NT$2.1），batch 半價 $0.0335（≈ NT$1.1）。
一支片 4 張＝ **$0.27 ≈ NT$8.5**。日更一個月 **NT$255**。

為了省一個月 NT$255，換來今天這種一整天的災難——不划算到不需要猶豫。

**⑤ 發布改走 YouTube Data API v3，把 `publish_video.py` 整支換掉**

現在的 `publish_video.py` 在用 Playwright 點 youtube.com/upload，靠中文按鈕文字
（「選取檔案」「顯示更多」「下一步」「發布」）定位——**YouTube 改一次介面文案就全壞**。

好消息：YouTube Data API v3 **免費**，而且 Google 在 2026 年 6 月改了配額規則——
上傳不再從共用的 10,000 units 扣，預設約 **100 支/天**（舊資料會說一天只能傳 6 支，
那是 2025 年 12 月前的規則）。你一天發 1–5 支，完全不用擔心。

API 版還能直接做到網頁版做不到的事：
- `status.publishAt` 一次把 10 支排到未來 10 天，**發布這步從此完全不需要人**
- `status.selfDeclaredMadeForKids`、`selfDeclaredAltered`（AI 揭露）用參數設定，不用點 radio button
- 上傳完直接拿到 videoId，不用從 DOM 撈 href

**⑥ 保留本機 ComfyUI，但改變它的角色**

不要當「Veo 掛了的備胎」（8/8 和 8/10 兩次證明這條路只會生出爛片）。
改成 **prompt 預覽台**：花 2 分鐘生一段 5 秒草稿，確認構圖和動作演得出來，
再花錢送 API。這樣硬體沒浪費，而且它做的事**不在發布路徑上**——壞了也不會斷更。

### 檔案級的去留清單

| 動作 | 檔案 |
|---|---|
| **退休** | `gbot.py`、`browser_lock.py`、`make_scene.py`、`make_scene_ref.py`、`make_video_cloud.py`、`veo_resume.py`、`veo_diag.py`、`diag*.py`、`test_*.py`、`publish_video.py` |
| **改造保留** | `pipeline.py`（邏輯搬進 `worker.py`）、`state.json`（升級成 jobs 表）、`daily_report.py` |
| **新增** | `gen_image.py`、`gen_video.py`、`precheck.py`、`review_bot.py`、`publish_api.py`、`crosspost.py` |

砍掉的行數會比新增的多。**這是重構做對了的訊號。**

---

## Q4. 審核關卡怎麼設計才不會變成瓶頸？

今天的實測：02:06 生好 → 11:24 核准，**卡了 9 小時 18 分**。

### 先講一個反直覺的結論：不要試圖「加快審核」

你睡覺就是會睡 8 小時，這件事沒得優化。
真正的問題是**審核站在發布的關鍵路徑上**——這是排程設計錯誤。

四層設計，由治本到治標：

### 第一層（治本）：用存貨把審核踢出關鍵路徑 ⭐

有 14 支存貨時，一支卡 9 小時**完全不影響任何一天的發布**。
`auto/README.md` 現在寫著「有片子在待審時，產線不會再往下生新的」——
這條規則在存貨制下必須拿掉，它正是製造瓶頸的元凶。

**這一層做到了，下面三層都只是加分。**

### 第二層：機器預審，讓爛片在你睡覺時就被篩掉

你今天的所有審核否決，理由都是同一類：多了一隻狗、黑點眉不見了、長出人手。
**這些機器看得出來。**

`precheck.py`：抽 4 張關鍵幀 → 丟 Gemini Flash 視覺模型，問四個是非題：

```
1. 畫面裡有幾隻狗？（應為 2）
2. 吉娃娃眉毛上有沒有兩個黑點？
3. 有沒有出現人的手或人臉？
4. 哈士奇是不是黑白色、藍眼睛？
```

任一題不對 → **自動退回重生，不吵你**。
成本一支不到 NT$1，卻能讓 80% 的爛片在凌晨自己消失。
你早上起床看到的，是已經過機檢的片——審核通過率會從約 50% 跳到 90%+。

### 第三層：把審核搬到手機，一支 10 秒

不要叫人開電腦、開資料夾、播影片。改成：

1. 片子生好 → 自動拼一張**四格圖**（0%／33%／66%／100% 幀）
2. 推到 LINE 或 Telegram（你已經做過 LINE bot，這條路熟）
3. 訊息長這樣：

```
[四格圖]
D5S1《Husky Steals The Whole Pizza 🍕》
機檢：✅ 2 狗 ✅ 黑點眉 ✅ 無人手 ✅ 品種對
排定 8/15 08:00

回 1 = 過   2 = 重生   3 = 我要看影片
```

躺在床上 10 秒解決。**9 小時 18 分變成「早上刷手機時順手按一下」。**

### 第四層：安全網，最壞情況也不斷更

如果你 12 小時沒回、而且存貨低於 3 支：
機檢全綠的片 → **自動上傳並排程，但設為「不公開」**。
你起床後看到通知，覺得可以就一鍵轉公開。

這樣最壞的結果是「延後幾小時公開」，不是「斷更」。
**永遠不要讓自動系統在沒人看過的情況下公開發布**——這條 8/8 的教訓要留著。

---

## Q5. 五個平台一稿多投怎麼做？

### 先講壞消息：自己串 OAuth 是地獄

| 平台 | 自己串的難度 | 卡在哪 |
|---|---|---|
| **YouTube Shorts** | 🟢 簡單 | Data API v3 免費，約 100 支/天。**這個自己寫** |
| **TikTok** | 🔴 很難 | Content Posting API 未過審只能發 **SELF_ONLY（私人）**，而且未過審的 client 24 小時只准 5 個帳號發文。要公開必須送 audit |
| **Instagram Reels** | 🟡 中等 | 要 Business/Creator 帳號 ＋ 綁 Facebook 專頁 ＋ 走 Graph API |
| **Facebook Reels** | 🟡 中等 | 同上，共用一套 |
| **X / Threads** | 🟡 中等 | X 的 API 有月費且影片上傳限制多 |

TikTok 那條就足以否決「自己全串」的方案——**光是等 audit 就可能兩三週，而且會被打回。**

### 建議：YouTube 自己寫，其餘四個租

**Upload-Post**：免費層 2 個 profile／10 支/月；付費 **$16/月（≈ NT$504）** 不限量、5 個 profile。
選它的理由是它**內建 ffmpeg 影片正規化**——TikTok 和 IG Reels 對編碼格式很挑，
自己處理會反覆碰壁，它幫你擋掉。而且有現成的 n8n / Make 節點。

**替代方案**：
- **Blotato** $29/月（≈ NT$914），20 個帳號、AI 寫文案，功能多但你現在用不到
- **Postiz** 開源可自架（$0）或 hosted $29/月——省錢但要自己維護一台伺服器，
  **不建議**：你要的是少一件事要顧，不是多一台機器要修

### 三個一稿多投的坑

**坑一：一稿多投 ≠ 一個檔案丟五個地方。**
各平台會偵測別家浮水印並降流量。從 YouTube 下載再丟 TikTok 是最糟的做法——
一定要**從母帶各自輸出**，並且各平台用各自的標題與 hashtag。

**坑二：AI 揭露要各平台各自勾。**
Veo 輸出內建 SynthID，YouTube 那邊要勾「經變造或合成的內容」，
TikTok / IG 也各有自己的 AI 標示欄位。這個不能省，被抓到比較痛。

**坑三：多平台會放大「模板化內容」的風險。**
同一支片配同一句文案發五個地方，正好符合 2026-07 那條「不真實內容」政策的判定特徵。
至少讓各平台的文案不同。

---

## Q6. 預算有限，先花哪一筆？

### 花錢優先序

| 順位 | 花什麼 | 月費 | 買到什麼 |
|---|---|---|---|
| **1** | **Veo 3.1 API（Fast 或 Lite）** | **NT$820–1,400** | 買掉整套瀏覽器遙控技術債 ＋ 畫質 ＋ 角色一致性 |
| 2 | Upload-Post | NT$504 | 買掉四個平台的 OAuth 地獄 |
| 3 | RAM 一條 16GB | NT$4,888（一次性） | 系統穩定（詳見「選型-硬體路線」） |
| — | **不要花** | — | 顯卡、剪輯軟體訂閱、AI 文案工具、頻道代管 |

### 為什麼第一筆一定是 Veo API

它是**唯一同時消滅「機器失敗」和「人力消耗」的支出**。其他每一筆都只解決一半。

算一下（Veo 3.1 Fast 720p **$0.10/秒**、Lite 720p **$0.05/秒**，官方定價）：

| 方案 | 一支 12 秒 | 日更月費 | 備註 |
|---|---|---|---|
| Veo 3.1 Lite | $0.60 ≈ NT$19 | **≈ NT$819**（含生圖） | 720p，畫質夠 Shorts 用 |
| Veo 3.1 Fast | $1.20 ≈ NT$38 | **≈ NT$1,386**（含生圖） | 建議從這個開始 |
| Veo 3.1 Standard | $4.80 ≈ NT$151 | NT$4,788 | 過度投資，不需要 |
| Google AI Pro（網頁版） | ≈ NT$12.6 | NT$630 | **最便宜，但要走網頁——今天壞掉的就是這條路** |

**AI Pro 每支便宜 NT$25，但它把你綁回瀏覽器遙控。**
今天為了跟瀏覽器搏鬥，你至少燒掉 3 小時。一個月省下的 NT$750，
換算下來你的時薪不到 NT$100。**不要為了這個錢把技術債留著。**

### 這條線最值錢的一筆錢，其實是 0 元

排在所有付費項目前面的，是**建立 14 支存貨**。
它不用錢，只需要一次「認真做一個週日下午」的決心，
但它同時解決了審核瓶頸、API 失敗、你生病、和你想放棄的那一天。

**存貨是這條產線唯一的減震器。** 沒有它，上面每一筆錢都在買脆弱的東西。

---

## 兩週落地清單

**第 1 週（建產線，接受這週少發幾支）**

1. 申請 Google AI Studio API key，`pip install google-genai`，先花 NT$19 生一支測試片驗證
2. 寫 `gen_video.py`（Veo 3.1 Fast，poll operation，operation name 存檔）
3. 寫 `publish_api.py`（YouTube Data API v3，含 `publishAt` 排程）
4. 關掉 `TacoNova-Pipeline` 工作排程器，`browser_lock.py` 退休

**第 2 週（建存貨、接手機）**

5. 一次選 14 個題材，跑第一場批次生產
6. 寫 `precheck.py`（Gemini Flash 四題視覺檢查）
7. 接 LINE／Telegram 審核 bot，四格圖 ＋ 回 1/2
8. 訂 Upload-Post，先接 TikTok 一個平台試水溫

**第 3 週開始：週日下午開工，其餘六天不碰它。**

---

## 資料來源

- [Gemini Developer API pricing（官方，Veo 與生圖定價）](https://ai.google.dev/gemini-api/docs/pricing)
- [YouTube API Quota Limits 2026 — Phyllo](https://www.getphyllo.com/post/youtube-api-limits-how-to-calculate-api-usage-cost-and-fix-exceeded-api-quota)
- [YouTube Upload API: Videos & Shorts (2026) — Postproxy](https://postproxy.dev/blog/youtube-upload-api-guide/)
- [TikTok Content Posting API Guide（官方）](https://developers.tiktok.com/doc/content-posting-api-get-started)
- [TikTok API Approval: Audit, Scopes, Rejections — bundle.social](https://bundle.social/blog/tiktok-api-approval)
- [TikTok Content Posting API in 2026: Direct Post, Audit, and Alternatives — PostPeer](https://www.postpeer.dev/blog/best-tiktok-posting-api)
- [Which Social Media APIs Support Multi-Platform Posting — Buffer](https://buffer.com/resources/social-media-api-multi-platform-posting/)
- [5 Best Upload-Post Alternatives（含 Upload-Post／Blotato 定價）— Blotato](https://www.blotato.com/blog/upload-post-alternatives)
- [Free Social Media Scheduling API with n8n — Postiz](https://postiz.com/blog/free-social-media-scheduling-api-n8n-postiz-buffer-blotato-alternative)
- [How to bulk schedule YouTube Shorts in 2026 — Schedulala](https://schedulala.com/blog/bulk-schedule-youtube-shorts)
- [The Creator Burnout Epidemic — The Creator Economy](https://thecreatoreconomy.com/post/creator-burnout-epidemic-mental-health-2026)（⚠️ 行銷內容站，數字當方向參考，不當證據）

**專案內部證據**：`auto/pipeline.log`（8/10 三次失敗的時間戳）、`auto/README.md`（產線現況與三個已知限制）、`auto/state.json`（發布紀錄）、`auto/publish_video.py`（現行的瀏覽器上傳實作）、`交接-2026-08-10.md`
