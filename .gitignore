# ── 影音素材：不進版控 ───────────────────────────────
# 成片、草稿、素材片段約 65 MB。都可以從 Veo / 本機重跑產生，
# 或已經另外備份，放進 git 只會把 repo 撐大。
*.mp4

# ── 圖片：預設不進版控 ──────────────────────────────
# 縮圖（*-thumb.jpg）是從 mp4 抽格出來的衍生檔，
# 頻道視覺（channel-avatar.jpg、channel-banner.png）已上傳到 YouTube。
*.png
*.jpg
*.jpeg

# ── 例外：character/ 定裝照「要」進版控 ───────────────
# taco-ref-*.jpg、duo-scene-*.jpg 是生成正片時餵給 Veo 的參考圖，
# 是必要輸入且不可遺失 —— 這些是 AI 生成的，重跑不會得到同一隻 Taco，
# 弄丟就是永久弄丟，不像縮圖可以再抽一次。
# 九張合計約 8.6 MB，對純本地 repo 來說完全可以接受，
# 所以這裡反向排除，讓它們受版控保護。
!character/*.jpg

# ── agent 工具設定，不是專案內容 ─────────────────────
# 每個 agent 跑起來會自己生一份，進版控只會一直有雜訊 diff。
.claude/

# ── Windows 桌面雜物 ────────────────────────────────
Thumbs.db
desktop.ini
.DS_Store
