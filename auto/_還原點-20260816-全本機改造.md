# 還原點：2026-08-16 全本機改造

這份檔案存在的理由：財富密碼沒掛 Git remote，改壞了不能 `git revert`。
下面三段是**改動前的原始程式碼**，要還原直接照著貼回去就好。

改動的三個檔：`pipeline.py`、`plan_model.py`、`model_policy.json`

---

## 症狀對照表（怪怪的時候先查這個）

| 你看到的症狀 | 可能是哪個改動害的 | 跳到 |
|---|---|---|
| 影片裡 Taco 的**黑點眉不見了**／臉不對／畫質突然變差，但你什麼都沒改 | ⚠️ **靜默降級**：Edge 沒起來，產線自己改用本機生圖 | [還原 1](#還原-1) |
| 產線一直說「⛔ 本機做不到」，什麼都不生、進度卡住 | plan_model 守門太嚴 | [還原 2](#還原-2) |
| 產線停在「連續失敗 3 次，等賢賢看過再說」，但其實只是 ComfyUI 抽風 | `LOCAL_MAX_FAILS = 3` 這個數字是拍的 | [還原 3](#還原-3) |
| 影片生出來但畫面歪／崩壞 | 這不是這次改動害的，是本機模型本來的極限 | 看 `MODEL_CAPABILITY.md` |

**最需要盯的是第一個。** 它是三個裡唯一會「安靜地變爛」的——
片子照樣生出來、照樣送審，log 只有一行「Edge/CDP 起不來 → 這輪生圖直接走本機」。
其他兩個都是「停下來吵你」，反而安全。

---

## 還原 1

**改動**：Edge/CDP 起不來時不再中止，改走本機生圖。
**風險**：本機生圖畫不出 Taco 的黑點眉（實測），等於招牌不見了還照樣送審。
**位置**：`pipeline.py`，`cmd_tick()` 裡，plan_model 那段之後。

現在長這樣：

```python
    # Edge 只剩生圖在用了（影片已全本機）。起不來就直接走本機生圖，不要卡住整條產線。
    edge_ok = ensure_edge()
    if not edge_ok:
        log("  Edge/CDP 起不來 → 這輪生圖直接走本機")
```

**改回原本**：

```python
    if not ensure_edge():
        return
```

同時要把生圖那段改回去。現在長這樣：

```python
        ok = False
        if edge_ok:
            ok, _ = run("make_scene.py", prompt_file, scene)
        if not ok or not scene.exists():
```

**改回原本**：

```python
        ok, _ = run("make_scene.py", prompt_file, scene)
        if not ok or not scene.exists():
```

---

## 還原 2

**改動**：把 `plan_model` 接進產線當守門員（原本它只是一支手動跑的獨立工具，pipeline 沒有 import 它）。
**位置**：`pipeline.py` import 區 + `cmd_tick()`。

**只想暫時放行某一支**（不用改程式）：把該支的判斷結果手動寫進 `state.json`，
或直接改 `schedule.json` 的 `videoPrompt` 繞開低分關鍵字 —— 這是設計上預期的用法。

**要整個拿掉**：刪掉 import 區的 `import plan_model`，以及下面這整段：

```python
    # 這鏡本機拍不拍得出來？拍不出來就不要浪費算力，直接請賢賢改劇本。
    # 2026-08-16 全本機之後，plan_model 的輸出從「選模型」變成「能不能拍」。
    plan = plan_model.decide(item.get("videoPrompt") or item["title"])
    if plan["model"] == "BLOCKED":
        ...（到 log("  模型判斷：...") 那三行為止全刪）
```

注意：影片那步有一行 `steps = plan_model.POLICY["local_best_config"]["steps"]`，
拿掉 import 的話要一起改成寫死 `steps = 8`。

---

## 還原 3

**改動**：新增 `LOCAL_MAX_FAILS = 3`，取代原本的 Veo 重試退避機制。
**位置**：`pipeline.py` 常數區。

`3` 這個數字**沒有實測根據**，是拍腦袋定的。覺得太容易停就調大。
注意：ComfyUI 起不來**不算**失敗次數（已寫在程式裡），所以會累計的只有真正的生片失敗。

要清掉某支已經累積的失敗次數：改 `state.json` 裡那支的 `local_fails` 歸零。

---

## 完整還原：影片那步改回 Veo

真的要整個回到雲端（`make_video_cloud.py` 和 `veo_quota.py` 都還在，沒刪）：

```python
    # 3) 影片 —— 只走 Veo。本機 ComfyUI 畫面會歪掉，不准拿來發布。
    video = CLIPS / f"{key}.mp4"
    if video.exists() and st.get("source") != "veo":
        log("  發現來路不明的同名影片，砍掉重生")
        video.unlink()
    if not video.exists():
        vp = CLIPS / f"{key}_video.txt"
        vp.write_text(item["videoPrompt"], encoding="utf-8")
        ok, out = run("make_video_cloud.py", scene, vp, video, timeout=1200)
        if not ok and "SKIP:" in out:
            log("  遙控瀏覽器被別人佔用，這輪讓路（不算失敗）")
            return
        if not ok or not video.exists():
            fails = st.get("veo_fails", 0) + 1
            wait_h = veo_backoff_h(out, fails)
            st["veo_fails"] = fails
            st["veo_retry_after"] = (datetime.now() + timedelta(hours=wait_h)).isoformat(timespec="seconds")
            state[key] = st
            save(STATE, state)
            why = "額度用完，等重置" if "額度用完" in out else "服務抽風"
            log(f"  Veo 失敗（第 {fails} 次，{why}），{wait_h * 60:.0f} 分鐘後再試。不改用本機。")
            return
    st.update({"video": str(video), "source": "veo", "veo_fails": 0})
    log(f"  影片 OK（Veo）：{video.name}")
```

還有前面那段 Veo 等待邏輯（在 `LATE_TOLERANCE_H` 判斷之後）：

```python
    # Veo 剛失敗過就先別急著再試，等額度回來
    nxt = st.get("veo_retry_after")
    if nxt and datetime.now() < datetime.fromisoformat(nxt):
        log(f"  Veo 上次失敗，{nxt[:16]} 之後再試")
        return
    if st.get("veo_fails", 0) >= VEO_MAX_FAILS:
        log(f"  Veo 連續失敗 {st['veo_fails']} 次，停止自動重試 —— 等賢賢處理")
        return
```

以及 `to_review(key, item, video, "Veo")`。

---

## 這次改動沒驗到的部分

**沒有跑過一支完整的片。** 驗過的只有：語法編譯、模組載入、
19 支片單的模型判斷、log 格式化不會 KeyError、`pipeline.py status` 正常。

**端到端（生圖 → 生片 → 送審）一次都沒跑過。** 第一次跑 `tick` 要盯著看。
